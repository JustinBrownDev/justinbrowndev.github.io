--- main.js.before-grass-hotfix	2026-08-29 19:52:35.680319412 +0000
+++ main.js	2026-08-29 19:52:36.171888084 +0000
@@ -3088,6 +3088,8 @@
 const unitPlaneGeo = new THREE.PlaneGeometry(1, 1);
 // Shared park/gallery bench material must exist before signature-site generation.
 const sharedBenchMaterial = new THREE.MeshStandardMaterial({ color: 0x5a4530, roughness: 0.9 });
+// Lazy park material backing storage must exist before the synchronous special-feature pass.
+let _sharedGrassMaterial = null;
 
 // generic one-time vertex jitter — perturbs a geometry's own vertices at
 // creation so cheap primitives (crates, cans, machines) read as hand-built
@@ -9729,7 +9731,6 @@
     return 1.4;
 }
 
-let _sharedGrassMaterial = null;
 function sharedGrassMaterial() {
     if (_sharedGrassMaterial) return _sharedGrassMaterial;
     const lr = localRng(hashString32(`${SEED}:shared-grass`));
