TDZ boot hotfix validation
==========================

Browser-reported regression reproduced by static full-boot audit:
  special-feature park loop -> addPark -> sharedGrassMaterial -> _sharedGrassMaterial
  backing `let` initialized after the park loop.

Fix:
  - moved `let _sharedGrassMaterial = null` into the early shared-resource section
  - retained lazy material/texture creation inside sharedGrassMaterial()
  - replaced the old building-only TDZ audit with a transitive full synchronous-boot audit

Post-fix audit:
  executable roots with local calls: 64
  hazards: 0

Checks:
  node --check main.js: PASS
  node --check player-physics.js: PASS
  node --check city-performance.js: PASS
  player-physics-selftest.mjs: PASS
  city-performance-selftest.mjs: PASS
