/test FULL-FIDELITY PROGRESSIVE RUNTIME VALIDATION
2026-08-29

BASE
- Current full-site quantitative-parameter-lab ZIP supplied by user/library.
- Root main.js intentionally unchanged.
- /test/main.js is a scheduling fork copied from that exact root main.js.

HARD INVARIANT
/test may change WHEN root work executes, never WHAT world/content logic exists.
No surrogate city, no regular-grid proxy buildings, no replacement AABB world,
no LOTS=6 lattice, no world-worker.js, no alternate app.js.

PROGRESSIVE BEHAVIOR
- Noise corpus module fetches start immediately, same source files as root.
- Renderer/camera/lighting + PointerLockControls are live before corpus await.
- Root boot overlay is reduced to a non-blocking translucent HUD on /test.
- Desktop WASD/mouse-look works during load.
- Once the real maze topology exists, bootstrap movement is constrained to real
  open maze cells even if their surrounding building meshes have not painted.
- Authoritative BuildingSites materialize in original order using the exact
  buildSignatureSite/addBuildingSite implementations.
- Generation yields after completed work slices when frame budget is exhausted
  or input is pending. No RNG is consumed by scheduler code.
- Real street/alley surface generation yields by completed rows.
- Existing near-first deferred decoration remains enabled.
- Traversal validation and real static-world optimizer still run.
- Camera/control state hands directly to real createPlayerPhysics runtime.

TUNING
/test/?frameBudget=2..12
Default: 7ms desktop, 5ms mobile/potato. This affects scheduling only.

MECHANICAL TESTS RUN
PASS node --check main.js
PASS node --check test/main.js
PASS node player-physics-selftest.mjs
PASS node city-performance-selftest.mjs
PASS node parameter-system-selftest.mjs
PASS node parameter-catalog-selftest.mjs
PASS node test/full-fidelity-selftest.mjs

FIDELITY SELF-TEST BYTE-COMPARES THESE FUNCTION BODIES ROOT VS /test
- worldToCellIndex
- buildSignaturePlaceholder
- buildSignatureSite
- addBuildingSite
- buildRooftopCatwalks
- buildHangingBridges
- buildTraversalGraph
- validateTraversal

ROOT main.js SHA256
36ee74459a6583217ea87d36f757e4a71db94376a7d8ab41ae6967554e4f79ef

NOTE ON VISUAL BROWSER QA
The build container's managed Chromium blocks localhost by organization policy,
so no claim of a visual browser pass is made here. Source/module/syntax and all
available repo self-tests were run instead. The /test HUD exposes live phase,
progress, and full-runtime handoff for immediate manual verification after push.
