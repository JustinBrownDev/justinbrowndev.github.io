# 2026-08-31 loose-thread checkpoint

This directory is intentionally a checkpoint, not a live feature cut. It preserves every known off-main artifact from the two converging work streams before further implementation.

Start with `LOOSE-THREADS-SUPERCUT.md`, then `THREAD-STATUS.md` and `CHECKPOINT-BASE.md`.
