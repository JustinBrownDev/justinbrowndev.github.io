
import { parameterNumber, registerLiteralScope } from "./numeric-parameters.js";
 
let __qp0=parameterNumber("n.perf.8cad4fe20aa6",4,true,"perf",0);
let __qp1=parameterNumber("n.perf.7f6e33df1fa3",0.25,true,"perf",1);
let __qp2=parameterNumber("n.perf.43ef688a29d8",0,true,"perf",2);
let __qp3=parameterNumber("n.perf.838c875816b6",0,true,"perf",3);
let __qp4=parameterNumber("n.perf.434296da924c",1,true,"perf",4);
let __qp5=parameterNumber("n.perf.07aeb6532a4c",0,true,"perf",5);
let __qp6=parameterNumber("n.perf.b9170b3e4529",1,true,"perf",6);
let __qp7=parameterNumber("n.perf.7344691f2b9d",0,true,"perf",7);
let __qp8=parameterNumber("n.perf.56a5f541ed6e",0,true,"perf",8);
let __qp9=parameterNumber("n.perf.98e1bccc5b58",1,true,"perf",9);
let __qp10=parameterNumber("n.perf.2d42c606d94d",0,true,"perf",10);
let __qp11=parameterNumber("n.perf.c80754aaeead",1,true,"perf",11);
let __qp12=parameterNumber("n.perf.7626d8fd32f3",0,true,"perf",12);
let __qp13=parameterNumber("n.perf.a5d29dce972a",1,true,"perf",13);
let __qp14=parameterNumber("n.perf.6dde36f91fba",0,true,"perf",14);
let __qp15=parameterNumber("n.perf.59b3c15e7f43",1,true,"perf",15);
let __qp16=parameterNumber("n.perf.0668a7c764b6",1,true,"perf",16);
let __qp17=parameterNumber("n.perf.07293744b16a",0,true,"perf",17);
let __qp18=parameterNumber("n.perf.e57a5b2d0e08",1,true,"perf",18);
let __qp19=parameterNumber("n.perf.842f255ae4b9",0,true,"perf",19);
let __qp20=parameterNumber("n.perf.f1039e435c91",1,true,"perf",20);
let __qp21=parameterNumber("n.perf.8314400da41d",0,true,"perf",21);
let __qp22=parameterNumber("n.perf.825b19db65a1",1,true,"perf",22);
let __qp23=parameterNumber("n.perf.3503294811dc",0,true,"perf",23);
let __qp24=parameterNumber("n.perf.55dae53d71ed",1,true,"perf",24);
let __qp25=parameterNumber("n.perf.e63706331536",0,true,"perf",25);
let __qp26=parameterNumber("n.perf.8cd4cd2e066b",1,true,"perf",26);
let __qp27=parameterNumber("n.perf.c85fee9a7b84",0,true,"perf",27);
let __qp28=parameterNumber("n.perf.956f14ceede1",1,true,"perf",28);
let __qp29=parameterNumber("n.perf.96db30868dfc",0,true,"perf",29);
let __qp30=parameterNumber("n.perf.b7d0a9abb11e",0,true,"perf",30);
let __qp31=parameterNumber("n.perf.3c1092389f73",0,true,"perf",31);
let __qp32=parameterNumber("n.perf.cd6b4a541416",0,true,"perf",32);
let __qp33=parameterNumber("n.perf.1842eb6dbe20",1,true,"perf",33);
let __qp34=parameterNumber("n.perf.c874dc849221",0,true,"perf",34);
let __qp35=parameterNumber("n.perf.ec8aa185e7a4",0,true,"perf",35);
let __qp36=parameterNumber("n.perf.a3361f38b78a",0,true,"perf",36);
let __qp37=parameterNumber("n.perf.42cd85c3c030",0,true,"perf",37);
let __qp38=parameterNumber("n.perf.1a9f65fa22ca",0,true,"perf",38);
let __qp39=parameterNumber("n.perf.a3b4fb7e5d71",0,true,"perf",39);
let __qp40=parameterNumber("n.perf.89eb06d5b2c4",0,true,"perf",40);
let __qp41=parameterNumber("n.perf.64ea8542cbf5",0,true,"perf",41);
let __qp42=parameterNumber("n.perf.d3be13f82633",0,true,"perf",42);
let __qp43=parameterNumber("n.perf.3b4f92d115e8",65535,true,"perf",43);
let __qp44=parameterNumber("n.perf.d87e9a6b8b51",0,true,"perf",44);
let __qp45=parameterNumber("n.perf.563038ecf837",0,true,"perf",45);
let __qp46=parameterNumber("n.perf.808918eb27ee",0,true,"perf",46);
let __qp47=parameterNumber("n.perf.98f5cca5aaa6",1,true,"perf",47);
let __qp48=parameterNumber("n.perf.31d0f64e86c1",1,true,"perf",48);
let __qp49=parameterNumber("n.perf.c4953a09d3e3",0,true,"perf",49);
let __qp50=parameterNumber("n.perf.fa63933d3c3b",3,true,"perf",50);
let __qp51=parameterNumber("n.perf.5a5e95c01e25",120000,true,"perf",51);
let __qp52=parameterNumber("n.perf.69cbf4e492e1",0,true,"perf",52);
let __qp53=parameterNumber("n.perf.550b9deb7b02",1,true,"perf",53);
let __qp54=parameterNumber("n.perf.f9fe792b224a",0,true,"perf",54);
let __qp55=parameterNumber("n.perf.09ec127efb15",1,true,"perf",55);
let __qp56=parameterNumber("n.perf.d5cca15872a6",0,true,"perf",56);
let __qp57=parameterNumber("n.perf.958aaaa167e9",1,true,"perf",57);
let __qp58=parameterNumber("n.perf.4830413c57a1",0,true,"perf",58);
let __qp59=parameterNumber("n.perf.65196b4dde45",0,true,"perf",59);
let __qp60=parameterNumber("n.perf.11c664444d0a",0,true,"perf",60);
let __qp61=parameterNumber("n.perf.1ec9202691e5",0,true,"perf",61);
let __qp62=parameterNumber("n.perf.bc8b473a1c2e",0,true,"perf",62);
let __qp63=parameterNumber("n.perf.cff7c70cf83e",0,true,"perf",63);
let __qp64=parameterNumber("n.perf.61200bc68f88",0,true,"perf",64);
let __qp65=parameterNumber("n.perf.273b7db62eb2",0,true,"perf",65);
let __qp66=parameterNumber("n.perf.8903daa0b331",0,true,"perf",66);
let __qp67=parameterNumber("n.perf.d934a61ced2f",0,true,"perf",67);
let __qp68=parameterNumber("n.perf.3deaae7599f1",24,true,"perf",68);
let __qp69=parameterNumber("n.perf.00fe5d9e5221",2.5,true,"perf",69);
let __qp70=parameterNumber("n.perf.67a589b508a7",3,true,"perf",70);
let __qp71=parameterNumber("n.perf.46a3abaa5c85",120000,true,"perf",71);
let __qp72=parameterNumber("n.perf.35ff209e0ac2",0,true,"perf",72);
let __qp73=parameterNumber("n.perf.16092d57f5f5",0,true,"perf",73);
let __qp74=parameterNumber("n.perf.0fcdb35c9337",0,true,"perf",74);
let __qp75=parameterNumber("n.perf.c44dba724d5d",0,true,"perf",75);
let __qp76=parameterNumber("n.perf.5de66eae2051",0,true,"perf",76);
let __qp77=parameterNumber("n.perf.2039899af012",0.5,true,"perf",77);
let __qp78=parameterNumber("n.perf.73f575fb01f7",0.5,true,"perf",78);
let __qp79=parameterNumber("n.perf.76fc83245f49",0,true,"perf",79);
let __qp80=parameterNumber("n.perf.f2994701a398",0.01,true,"perf",80);
function __setQuantitativeLiteral(index,value){switch(index){case 0:__qp0=value;return true;case 1:__qp1=value;return true;case 2:__qp2=value;return true;case 3:__qp3=value;return true;case 4:__qp4=value;return true;case 5:__qp5=value;return true;case 6:__qp6=value;return true;case 7:__qp7=value;return true;case 8:__qp8=value;return true;case 9:__qp9=value;return true;case 10:__qp10=value;return true;case 11:__qp11=value;return true;case 12:__qp12=value;return true;case 13:__qp13=value;return true;case 14:__qp14=value;return true;case 15:__qp15=value;return true;case 16:__qp16=value;return true;case 17:__qp17=value;return true;case 18:__qp18=value;return true;case 19:__qp19=value;return true;case 20:__qp20=value;return true;case 21:__qp21=value;return true;case 22:__qp22=value;return true;case 23:__qp23=value;return true;case 24:__qp24=value;return true;case 25:__qp25=value;return true;case 26:__qp26=value;return true;case 27:__qp27=value;return true;case 28:__qp28=value;return true;case 29:__qp29=value;return true;case 30:__qp30=value;return true;case 31:__qp31=value;return true;case 32:__qp32=value;return true;case 33:__qp33=value;return true;case 34:__qp34=value;return true;case 35:__qp35=value;return true;case 36:__qp36=value;return true;case 37:__qp37=value;return true;case 38:__qp38=value;return true;case 39:__qp39=value;return true;case 40:__qp40=value;return true;case 41:__qp41=value;return true;case 42:__qp42=value;return true;case 43:__qp43=value;return true;case 44:__qp44=value;return true;case 45:__qp45=value;return true;case 46:__qp46=value;return true;case 47:__qp47=value;return true;case 48:__qp48=value;return true;case 49:__qp49=value;return true;case 50:__qp50=value;return true;case 51:__qp51=value;return true;case 52:__qp52=value;return true;case 53:__qp53=value;return true;case 54:__qp54=value;return true;case 55:__qp55=value;return true;case 56:__qp56=value;return true;case 57:__qp57=value;return true;case 58:__qp58=value;return true;case 59:__qp59=value;return true;case 60:__qp60=value;return true;case 61:__qp61=value;return true;case 62:__qp62=value;return true;case 63:__qp63=value;return true;case 64:__qp64=value;return true;case 65:__qp65=value;return true;case 66:__qp66=value;return true;case 67:__qp67=value;return true;case 68:__qp68=value;return true;case 69:__qp69=value;return true;case 70:__qp70=value;return true;case 71:__qp71=value;return true;case 72:__qp72=value;return true;case 73:__qp73=value;return true;case 74:__qp74=value;return true;case 75:__qp75=value;return true;case 76:__qp76=value;return true;case 77:__qp77=value;return true;case 78:__qp78=value;return true;case 79:__qp79=value;return true;case 80:__qp80=value;return true;default:return false;}}
registerLiteralScope("perf",__setQuantitativeLiteral);
 
 
 
 
 
 
 
 
 
 
 

export class SpatialHash2D {
    constructor(cellSize = __qp0) {
        this.cellSize = Math.max(__qp1, cellSize);
        this.columns = new Map();  
        this.itemCount = __qp2;
        this._querySeen = new Set();
    }

    _bucket(ix, iz, create = false) {
        let col = this.columns.get(ix);
        if (!col) {
            if (!create) return null;
            this.columns.set(ix, col = new Map());
        }
        let bucket = col.get(iz);
        if (!bucket && create) col.set(iz, bucket = []);
        return bucket || null;
    }

    insert(item, bounds) {
        if (!item || !bounds) return item;
        const ix0 = Math.floor(bounds.minX / this.cellSize);
        const ix1 = Math.floor(bounds.maxX / this.cellSize);
        const iz0 = Math.floor(bounds.minZ / this.cellSize);
        const iz1 = Math.floor(bounds.maxZ / this.cellSize);
        for (let ix = ix0; ix <= ix1; ix++) {
            for (let iz = iz0; iz <= iz1; iz++) this._bucket(ix, iz, true).push(item);
        }
        this.itemCount++;
        return item;
    }

    queryBounds(bounds, out = []) {
        out.length = __qp3;
        const ix0 = Math.floor(bounds.minX / this.cellSize);
        const ix1 = Math.floor(bounds.maxX / this.cellSize);
        const iz0 = Math.floor(bounds.minZ / this.cellSize);
        const iz1 = Math.floor(bounds.maxZ / this.cellSize);
        const seen = this._querySeen;
        seen.clear();
        for (let ix = ix0; ix <= ix1; ix++) {
            const col = this.columns.get(ix);
            if (!col) continue;
            for (let iz = iz0; iz <= iz1; iz++) {
                const bucket = col.get(iz);
                if (!bucket) continue;
                for (const item of bucket) {
                    if (seen.has(item)) continue;
                    seen.add(item);
                    out.push(item);
                }
            }
        }
        return out;
    }

    queryRadius(x, z, radius, out = []) {
        return this.queryBounds({
            minX: x - radius, maxX: x + radius,
            minZ: z - radius, maxZ: z + radius,
        }, out);
    }
}

function materialTextureKey(mat) {
    const slots = [
        'map', 'alphaMap', 'aoMap', 'bumpMap', 'normalMap', 'displacementMap',
        'emissiveMap', 'metalnessMap', 'roughnessMap', 'lightMap', 'envMap',
    ];
    return slots.map(k => mat[k]?.uuid || '-').join('/');
}

function materialKey(mat) {
    if (!mat || mat.isShaderMaterial || mat.isRawShaderMaterial) return null;
     
     
    if (Object.prototype.hasOwnProperty.call(mat, 'onBeforeCompile')
        || Object.prototype.hasOwnProperty.call(mat, 'customProgramCacheKey')) return null;
    const color = mat.color?.getHexString?.() ?? '-';
    const emissive = mat.emissive?.getHexString?.() ?? '-';
    return [
        mat.type, color, emissive, mat.emissiveIntensity ?? '-',
        mat.roughness ?? '-', mat.metalness ?? '-', mat.opacity ?? __qp4, mat.alphaTest ?? __qp5,
        mat.transparent ? __qp6 : __qp7, mat.side ?? __qp8, mat.depthWrite ? __qp9 : __qp10,
        mat.depthTest ? __qp11 : __qp12, mat.depthFunc ?? '-', mat.colorWrite ? __qp13 : __qp14,
        mat.blending ?? __qp15, mat.blendSrc ?? '-', mat.blendDst ?? '-', mat.blendEquation ?? '-',
        mat.vertexColors ? __qp16 : __qp17, mat.flatShading ? __qp18 : __qp19, mat.wireframe ? __qp20 : __qp21,
        mat.fog ? __qp22 : __qp23, mat.toneMapped ? __qp24 : __qp25, mat.dithering ? __qp26 : __qp27,
        mat.polygonOffset ? __qp28 : __qp29, mat.polygonOffsetFactor ?? __qp30, mat.polygonOffsetUnits ?? __qp31,
        materialTextureKey(mat),
    ].join('|');
}

function dedupeMaterialsInto(root, dynamicMaterials, canonical) {
    let replacements = __qp32;
    root.traverse(obj => {
        if (!obj.isMesh || Array.isArray(obj.material) || !obj.material) return;
        const mat = obj.material;
        if (dynamicMaterials?.has(mat)) return;
        const key = materialKey(mat);
        if (!key) return;
        const existing = canonical.get(key);
        if (existing && existing !== mat) {
            obj.material = existing;
            replacements++;
        } else {
            canonical.set(key, mat);
        }
    });
    return replacements;
}

function dedupeMaterials(scene, dynamicMaterials) {
    const canonical = new Map();
    const replacements = dedupeMaterialsInto(scene, dynamicMaterials, canonical);
    return { replacements, uniqueMaterials: canonical.size };
}

function attributeSignature(geometry) {
    const names = Object.keys(geometry.attributes).sort();
    if (!names.length || !geometry.attributes.position) return null;
    const parts = [geometry.index ? `index:${geometry.index.array?.constructor?.name || 'indexed'}` : 'index:none'];
    for (const name of names) {
        const a = geometry.attributes[name];
        if (!a || a.isInterleavedBufferAttribute || !a.array) return null;
        parts.push(`${name}:${a.itemSize}:${a.normalized ? __qp33 : __qp34}:${a.array.constructor.name}`);
    }
    return parts.join(',');
}

function transformedGeometry(mesh) {
     
     
     
     
     
    const geo = mesh.geometry.clone();
    geo.applyMatrix4(mesh.matrixWorld);
    return geo;
}

function mergeGeometryList(THREE, geometries) {
    if (!geometries.length) return null;
    const names = Object.keys(geometries[__qp35].attributes).sort();
    const indexed = !!geometries[__qp36].index;
    const totals = Object.fromEntries(names.map(n => [n, __qp37]));
    let totalVertices = __qp38;
    let totalIndices = __qp39;

    for (const geo of geometries) {
        const geoNames = Object.keys(geo.attributes).sort();
        if (geoNames.join('|') !== names.join('|') || !!geo.index !== indexed) return null;
        const position = geo.attributes.position;
        if (!position) return null;
        totalVertices += position.count;
        if (indexed) totalIndices += geo.index.count;
        for (const name of names) {
            const a = geo.attributes[name], base = geometries[__qp40].attributes[name];
            if (!a || a.itemSize !== base.itemSize || a.normalized !== base.normalized || a.array.constructor !== base.array.constructor) return null;
            totals[name] += a.array.length;
        }
    }

    const merged = new THREE.BufferGeometry();
    for (const name of names) {
        const base = geometries[__qp41].attributes[name];
        const ArrayType = base.array.constructor;
        const data = new ArrayType(totals[name]);
        let offset = __qp42;
        for (const geo of geometries) {
            data.set(geo.attributes[name].array, offset);
            offset += geo.attributes[name].array.length;
        }
        merged.setAttribute(name, new THREE.BufferAttribute(data, base.itemSize, base.normalized));
    }

    if (indexed) {
        const IndexType = totalVertices > __qp43 ? Uint32Array : Uint16Array;
        const indexData = new IndexType(totalIndices);
        let indexOffset = __qp44;
        let vertexOffset = __qp45;
        for (const geo of geometries) {
            const src = geo.index.array;
            for (let i = __qp46; i < src.length; i++) indexData[indexOffset + i] = src[i] + vertexOffset;
            indexOffset += src.length;
            vertexOffset += geo.attributes.position.count;
        }
        merged.setIndex(new THREE.BufferAttribute(indexData, __qp47));
    }

    merged.computeBoundingBox();
    merged.computeBoundingSphere();
    return merged;
}

function canBatchMesh(mesh, dynamicMaterials) {
    if (!mesh?.isMesh || mesh.isInstancedMesh || mesh.isSkinnedMesh || !mesh.visible || mesh.userData?.__perfBatched) return false;
    if (!mesh.geometry?.attributes?.position || mesh.children.length) return false;
    if (Array.isArray(mesh.material) || !mesh.material) return false;
    if (dynamicMaterials?.has(mesh.material)) return false;
    if (mesh.material.transparent || mesh.material.opacity < __qp48) return false;
    if (mesh.morphTargetInfluences || Object.keys(mesh.geometry.morphAttributes || {}).length) return false;
    const drawCount = mesh.geometry.index?.count ?? mesh.geometry.attributes.position.count;
    const drawRange = mesh.geometry.drawRange;
    if (drawRange && (drawRange.start !== __qp49 || (Number.isFinite(drawRange.count) && drawRange.count < drawCount))) return false;
    if (Object.prototype.hasOwnProperty.call(mesh, 'onBeforeRender')) return false;
    return attributeSignature(mesh.geometry) !== null;
}

function batchChunkMeshes(THREE, group, dynamicMaterials, minMeshes = __qp50, maxVertices = __qp51) {
     
     
     
    group.updateMatrixWorld(true);
    const buckets = new Map();
    group.traverse(child => {
        if (child === group || !canBatchMesh(child, dynamicMaterials)) return;
        const sig = attributeSignature(child.geometry);
        const key = [
            child.material.uuid, sig,
            child.renderOrder || __qp52,
            child.castShadow ? __qp53 : __qp54,
            child.receiveShadow ? __qp55 : __qp56,
            child.layers?.mask ?? __qp57,
        ].join('|');
        let bucket = buckets.get(key);
        if (!bucket) buckets.set(key, bucket = []);
        bucket.push(child);
    });

    let sourceMeshes = __qp58;
    let mergedMeshes = __qp59;
    for (const meshes of buckets.values()) {
        if (meshes.length < minMeshes) continue;
        let batch = [];
        let vertexCount = __qp60;

        const flush = () => {
            if (batch.length < minMeshes) { batch = []; vertexCount = __qp61; return; }
            const geos = batch.map(m => transformedGeometry(m));
            const mergedGeo = mergeGeometryList(THREE, geos);
            for (const g of geos) g.dispose?.();
            if (!mergedGeo) { batch = []; vertexCount = __qp62; return; }

            const sample = batch[__qp63];
            const merged = new THREE.Mesh(mergedGeo, sample.material);
            merged.name = `perf-batch:${batch.length}`;
            merged.renderOrder = sample.renderOrder;
            merged.castShadow = sample.castShadow;
            merged.receiveShadow = sample.receiveShadow;
            if (sample.layers && merged.layers) merged.layers.mask = sample.layers.mask;
            merged.frustumCulled = true;
            merged.userData.__perfBatched = true;
            group.add(merged);
            for (const m of batch) m.parent?.remove(m);
            sourceMeshes += batch.length;
            mergedMeshes++;
            batch = [];
            vertexCount = __qp64;
        };

        for (const mesh of meshes) {
            const count = mesh.geometry.index ? mesh.geometry.index.count : mesh.geometry.attributes.position.count;
            if (batch.length && vertexCount + count > maxVertices) flush();
            batch.push(mesh);
            vertexCount += count;
        }
        flush();
    }
    return { sourceMeshes, mergedMeshes, drawCallsSaved: Math.max(__qp65, sourceMeshes - mergedMeshes) };
}

function pruneEmptyGroups(root) {
    let removed = __qp66;
    const visit = (parent) => {
        for (const child of [...parent.children]) {
            if (child.children?.length) visit(child);
            if (child.isGroup && child.children.length === __qp67) {
                parent.remove(child);
                removed++;
            }
        }
    };
    visit(root);
    return removed;
}

function freezeObject(root) {
    root.updateMatrixWorld(true);
    root.traverse(obj => {
        if ('matrixAutoUpdate' in obj) obj.matrixAutoUpdate = false;
        if ('matrixWorldAutoUpdate' in obj) obj.matrixWorldAutoUpdate = false;
    });
}

 
 
 
 
 
 
export function createProgressiveStaticWorldOptimizer({
    THREE,
    scene,
    camera,
    rawSceneAdd,
    drawDistance,
    chunkSize = __qp68,
    dynamicRoots = new Set(),
    dynamicMaterials = new Set(),
    maxChunkSpan = chunkSize * __qp69,
    mergeMinMeshes = __qp70,
    mergeMaxVertices = __qp71,
    onChunkOptimized = null,
} = {}) {
    if (!THREE || !scene || !camera || !rawSceneAdd) throw new Error('createProgressiveStaticWorldOptimizer missing required scene arguments');

    const chunks = new Map();
    const visibleKeys = new Set();
    const pendingLateObjects = [];
    const dirtyChunks = new Set();
    const canonicalMaterials = new Map();
    const box = new THREE.Box3();
    const size = new THREE.Vector3();
    const center = new THREE.Vector3();
    let enabled = false;
    let incrementalMode = false;
    let optimizing = false;
    let phase = 'pending';
    let phaseDone = __qp72;
    let phaseTotal = __qp72;
    let lateObjects = __qp72;
    let materialReplacements = __qp72;
    let activeDrawDistance = drawDistance;
    let batchStats = { sourceMeshes: __qp73, mergedMeshes: __qp74, drawCallsSaved: __qp75, emptyGroupsPruned: __qp76 };
    let lastCx = Infinity, lastCz = Infinity;

    function chunkKey(ix, iz) { return `${ix},${iz}`; }
    function getChunk(ix, iz) {
        const key = chunkKey(ix, iz);
        let group = chunks.get(key);
        if (group) return group;
        group = new THREE.Group();
        group.name = `perf-chunk:${key}`;
        group.userData.__perfChunkGroup = true;
        group.userData.chunkX = ix;
        group.userData.chunkZ = iz;
        group.visible = false;
        rawSceneAdd(group);
        if (enabled) {
            group.updateMatrixWorld(true);
            group.matrixAutoUpdate = false;
            group.matrixWorldAutoUpdate = false;
            const centerX = (ix + __qp77) * chunkSize;
            const centerZ = (iz + __qp78) * chunkSize;
            const dx = centerX - camera.position.x;
            const dz = centerZ - camera.position.z;
            const maxDist = activeDrawDistance + Math.SQRT2 * chunkSize;
            if (dx * dx + dz * dz <= maxDist * maxDist) {
                group.visible = true;
                visibleKeys.add(key);
            }
        }
        chunks.set(key, group);
        return group;
    }

    function boundsOf(obj) {
        box.makeEmpty();
        try {
            if (obj.isInstancedMesh) {
                obj.computeBoundingBox?.();
                obj.computeBoundingSphere?.();
            }
            obj.updateMatrixWorld(true);
            box.setFromObject(obj);
        } catch {
            return null;
        }
        if (box.isEmpty()) return null;
        box.getSize(size);
        box.getCenter(center);
        return {
            minX: box.min.x, maxX: box.max.x, minZ: box.min.z, maxZ: box.max.z,
            width: size.x, depth: size.z, centerX: center.x, centerZ: center.z,
        };
    }

    function shouldStayGlobal(obj, b) {
        if (!b) return true;
        if (dynamicRoots.has(obj) || obj.isLight || obj.isCamera || obj.userData?.noSpatialChunk) return true;
        return b.width > maxChunkSpan || b.depth > maxChunkSpan;
    }

    function placeObject(obj, isLate = false) {
        if (!obj || obj.userData?.__perfChunkGroup || obj.userData?.worldChunkRoot) return false;
        const b = boundsOf(obj);
        if (shouldStayGlobal(obj, b)) {
            if (isLate && !dynamicRoots.has(obj)) freezeObject(obj);
            return false;
        }
        const ix = Math.floor(b.centerX / chunkSize);
        const iz = Math.floor(b.centerZ / chunkSize);
        const key = chunkKey(ix, iz);
        const group = getChunk(ix, iz);
        group.add(obj);
        dirtyChunks.add(key);
        if (isLate) {
            freezeObject(obj);
            lateObjects++;
            if (visibleKeys.has(chunkKey(ix, iz))) group.visible = true;
        }
        return true;
    }

    function roughDistanceSq(obj) {
        const x = obj?.userData?.detailCullCenterX ?? obj?.position?.x ?? camera.position.x;
        const z = obj?.userData?.detailCullCenterZ ?? obj?.position?.z ?? camera.position.z;
        const dx = x - camera.position.x, dz = z - camera.position.z;
        return dx * dx + dz * dz;
    }

    function chunkDistanceSq(group) {
        const x = (group.userData.chunkX + __qp77) * chunkSize;
        const z = (group.userData.chunkZ + __qp78) * chunkSize;
        const dx = x - camera.position.x, dz = z - camera.position.z;
        return dx * dx + dz * dz;
    }

    function updateVisibility(force = false) {
        if (!chunks.size) return;
        const cx = Math.floor(camera.position.x / chunkSize);
        const cz = Math.floor(camera.position.z / chunkSize);
        if (!force && cx === lastCx && cz === lastCz) return;
        lastCx = cx; lastCz = cz;

        const range = Math.ceil((activeDrawDistance + Math.SQRT2 * chunkSize) / chunkSize);
        const next = new Set();
        const maxDist = activeDrawDistance + Math.SQRT2 * chunkSize;
        const maxDistSq = maxDist * maxDist;
        for (let dx = -range; dx <= range; dx++) {
            for (let dz = -range; dz <= range; dz++) {
                const key = chunkKey(cx + dx, cz + dz);
                const group = chunks.get(key);
                if (!group) continue;
                const centerX = (cx + dx + __qp77) * chunkSize;
                const centerZ = (cz + dz + __qp78) * chunkSize;
                const ddx = centerX - camera.position.x, ddz = centerZ - camera.position.z;
                if (ddx * ddx + ddz * ddz > maxDistSq) continue;
                next.add(key);
                if (!visibleKeys.has(key)) group.visible = true;
            }
        }
        for (const key of visibleKeys) {
            if (!next.has(key)) {
                const group = chunks.get(key);
                if (group) group.visible = false;
            }
        }
        visibleKeys.clear();
        for (const key of next) visibleKeys.add(key);
    }

    function optimizeChunkGroup(group) {
        if (!group) return;
        const startedAt = typeof performance !== 'undefined' ? performance.now() : Date.now();
         
         
         
         
        if ('matrixAutoUpdate' in group) group.matrixAutoUpdate = true;
        if ('matrixWorldAutoUpdate' in group) group.matrixWorldAutoUpdate = true;
        group.updateMatrixWorld(true);
        materialReplacements += dedupeMaterialsInto(group, dynamicMaterials, canonicalMaterials);
        const stats = batchChunkMeshes(THREE, group, dynamicMaterials, mergeMinMeshes, mergeMaxVertices);
        batchStats.sourceMeshes += stats.sourceMeshes;
        batchStats.mergedMeshes += stats.mergedMeshes;
        batchStats.drawCallsSaved += stats.drawCallsSaved;
        batchStats.emptyGroupsPruned += pruneEmptyGroups(group);
        freezeObject(group);
        dirtyChunks.delete(chunkKey(group.userData.chunkX, group.userData.chunkZ));
        const endedAt = typeof performance !== 'undefined' ? performance.now() : Date.now();
        onChunkOptimized?.({
            ms: endedAt - startedAt,
            key: chunkKey(group.userData.chunkX, group.userData.chunkZ),
            sourceMeshes: stats.sourceMeshes,
            mergedMeshes: stats.mergedMeshes,
            drawCallsSaved: stats.drawCallsSaved,
        });
    }

    function beginIncremental() {
        if (enabled) return controller;
        enabled = true;
        incrementalMode = true;
        optimizing = false;
        phase = 'streaming world chunks';
        phaseDone = __qp72;
        phaseTotal = __qp72;
        updateVisibility(true);
        return controller;
    }

    function optimizeNearestDirtyChunk(phaseLabel = 'optimizing nearest streamed chunk') {
        if (!enabled) beginIncremental();
        let nearest = null;
        let nearestDistance = Infinity;
        for (const key of dirtyChunks) {
            const group = chunks.get(key);
            if (!group) continue;
            const distance = chunkDistanceSq(group);
            if (distance < nearestDistance) {
                nearestDistance = distance;
                nearest = group;
            }
        }
        if (!nearest) return false;
        optimizeChunkGroup(nearest);
        phase = phaseLabel;
        phaseDone++;
        phaseTotal = Math.max(phaseTotal, phaseDone + dirtyChunks.size);
        updateVisibility(true);
        return true;
    }

    async function flushDirtyChunks({ yieldControl = null, keys = null, phaseLabel = 'optimizing streamed chunks' } = {}) {
        if (!enabled) beginIncremental();
        const allowed = keys ? new Set(keys) : null;
        const pending = [];
        for (const key of dirtyChunks) {
            if (allowed && !allowed.has(key)) continue;
            const group = chunks.get(key);
            if (group) pending.push(group);
        }
        pending.sort((a, b) => chunkDistanceSq(b) - chunkDistanceSq(a));
        phaseTotal = pending.length;
        for (let done = __qp72; pending.length; done++) {
            const group = pending.pop();
            optimizeChunkGroup(group);
            phase = phaseLabel;
            phaseDone = done + 1;
            updateVisibility(true);
            if (yieldControl) await yieldControl(phaseLabel, done + 1, phaseTotal);
        }
        if (!dirtyChunks.size && incrementalMode) {
            phase = 'streaming world chunks';
            phaseDone = phaseTotal;
        }
        return controller;
    }

    async function finalizeIncremental({ yieldControl = null } = {}) {
        if (!enabled) beginIncremental();
        optimizing = true;

        while (pendingLateObjects.length) placeObject(pendingLateObjects.shift(), true);
        await flushDirtyChunks({ yieldControl, phaseLabel: 'finalizing streamed chunks' });

         
         
         
         
        const globalRoots = [...scene.children].filter(obj => !obj.userData?.__perfChunkGroup && !obj.userData?.worldChunkRoot && !dynamicRoots.has(obj));
        phaseTotal = globalRoots.length;
        for (let done = __qp72; done < globalRoots.length; done++) {
            const obj = globalRoots[done];
            materialReplacements += dedupeMaterialsInto(obj, dynamicMaterials, canonicalMaterials);
            freezeObject(obj);
            phase = 'finalizing streamed globals';
            phaseDone = done + 1;
            updateVisibility(true);
            if (yieldControl) await yieldControl(phase, done + 1, phaseTotal);
        }

        optimizing = false;
        phase = 'ready';
        phaseDone = phaseTotal;
        updateVisibility(true);
        return controller;
    }

    async function optimize({ yieldControl = null } = {}) {
        if (enabled || optimizing) return controller;
        optimizing = true;
        const yieldStep = async (nextPhase, done, total) => {
            phase = nextPhase;
            phaseDone = done;
            phaseTotal = total;
            updateVisibility(true);
            if (yieldControl) return !!(await yieldControl(nextPhase, done, total));
            return false;
        };

         
         
         
        const roots = [...scene.children]
            .filter(obj => !obj.userData?.__perfChunkGroup && !obj.userData?.worldChunkRoot)
            .sort((a, b) => roughDistanceSq(b) - roughDistanceSq(a));
        phaseTotal = roots.length;
        for (let done = __qp72; roots.length; done++) {
             
             
            const obj = roots.pop();
            placeObject(obj, false);
            await yieldStep('optimizing static world · spatial chunks', done + 1, phaseTotal);
        }

         
         
        const pendingChunks = [...chunks.values()];
        phaseTotal = pendingChunks.length;
        let reprioritizeChunks = true;
        for (let done = __qp72; pendingChunks.length; done++) {
            if (reprioritizeChunks) {
                pendingChunks.sort((a, b) => chunkDistanceSq(b) - chunkDistanceSq(a));
                reprioritizeChunks = false;
            }
            const group = pendingChunks.pop();
            optimizeChunkGroup(group);
            reprioritizeChunks = await yieldStep('optimizing static world · merging nearest chunks', done + 1, phaseTotal);
        }

         
         
         
        const globalRoots = [...scene.children].filter(obj => !obj.userData?.__perfChunkGroup && !obj.userData?.worldChunkRoot && !dynamicRoots.has(obj));
        phaseTotal = globalRoots.length;
        for (let done = __qp72; done < globalRoots.length; done++) {
            const obj = globalRoots[done];
            materialReplacements += dedupeMaterialsInto(obj, dynamicMaterials, canonicalMaterials);
            freezeObject(obj);
            await yieldStep('optimizing static world · freezing globals', done + 1, phaseTotal);
        }

         
         
         
        if ('matrixWorldAutoUpdate' in scene) scene.matrixWorldAutoUpdate = false;
        enabled = true;
        optimizing = false;

         
         
        while (pendingLateObjects.length) placeObject(pendingLateObjects.shift(), true);
        updateVisibility(true);
        phase = 'ready';
        phaseDone = phaseTotal;
        return controller;
    }

    const controller = {
        optimize,
        beginIncremental,
        flushDirtyChunks,
        optimizeNearestDirtyChunk,
        finalizeIncremental,
        updateVisibility,
        setDrawDistance(value) {
            const next = Number(value);
            if (!Number.isFinite(next) || next <= __qp79 || Math.abs(next - activeDrawDistance) < __qp80) return;
            activeDrawDistance = next;
            updateVisibility(true);
        },
        registerLateObject(obj) {
            if (!obj || obj.userData?.__perfChunkGroup || obj.userData?.worldChunkRoot) return;
            if (!enabled) {
                pendingLateObjects.push(obj);
                return;
            }
            placeObject(obj, true);
        },
        markDirtyObject(obj) {
            if (!obj || obj.userData?.worldChunkRoot) return false;
            // Program-family staging may temporarily mutate a leaf that is already
            // owned by an authored perf chunk. Dirty exactly that owner; never
            // capture generic worldChunkRoot content into the authored optimizer.
            let owner = obj.parent;
            while (owner && !owner.userData?.__perfChunkGroup) owner = owner.parent;
            if (owner?.userData?.__perfChunkGroup) {
                dirtyChunks.add(chunkKey(owner.userData.chunkX, owner.userData.chunkZ));
                return true;
            }
            const b = boundsOf(obj);
            if (shouldStayGlobal(obj, b)) return false;
            dirtyChunks.add(chunkKey(Math.floor(b.centerX / chunkSize), Math.floor(b.centerZ / chunkSize)));
            return true;
        },
        updateDynamicObject(obj) {
            if (!obj) return;
            obj.updateMatrix();
            obj.updateMatrixWorld(true);
        },
        getStats() {
            return {
                chunkSize,
                drawDistance: activeDrawDistance,
                chunks: chunks.size,
                visibleChunks: visibleKeys.size,
                lateObjects,
                materialReplacements,
                uniqueMaterials: canonicalMaterials.size,
                dirtyChunks: dirtyChunks.size,
                incrementalMode,
                optimizing,
                phase,
                phaseDone,
                phaseTotal,
                ...batchStats,
            };
        },
    };
    return controller;
}

export function createStaticWorldOptimizer({
    THREE,
    scene,
    camera,
    rawSceneAdd,
    drawDistance,
    chunkSize = __qp68,
    dynamicRoots = new Set(),
    dynamicMaterials = new Set(),
    maxChunkSpan = chunkSize * __qp69,
    mergeMinMeshes = __qp70,
    mergeMaxVertices = __qp71,
} = {}) {
    if (!THREE || !scene || !camera || !rawSceneAdd) throw new Error('createStaticWorldOptimizer missing required scene arguments');

    const chunks = new Map();
    const visibleKeys = new Set();
    const box = new THREE.Box3();
    const size = new THREE.Vector3();
    const center = new THREE.Vector3();
    let enabled = false;
    let lateObjects = __qp72;
    let activeDrawDistance = drawDistance;

    const canonicalMaterials = new Map();
    let materialReplacements = 0;
    for (const root of scene.children) {
        if (root.userData?.worldChunkRoot) continue;
        materialReplacements += dedupeMaterialsInto(root, dynamicMaterials, canonicalMaterials);
    }
    const materialStats = { replacements: materialReplacements, uniqueMaterials: canonicalMaterials.size };

    function chunkKey(ix, iz) { return `${ix},${iz}`; }
    function getChunk(ix, iz) {
        const key = chunkKey(ix, iz);
        let group = chunks.get(key);
        if (group) return group;
        group = new THREE.Group();
        group.name = `perf-chunk:${key}`;
        group.userData.__perfChunkGroup = true;
        group.userData.chunkX = ix;
        group.userData.chunkZ = iz;
        group.visible = false;
        rawSceneAdd(group);
         
         
         
        if (enabled) {
            group.updateMatrixWorld(true);
            group.matrixAutoUpdate = false;
            group.matrixWorldAutoUpdate = false;
            const centerX = (ix + __qp77) * chunkSize;
            const centerZ = (iz + __qp78) * chunkSize;
            const dx = centerX - camera.position.x;
            const dz = centerZ - camera.position.z;
            const maxDist = activeDrawDistance + Math.SQRT2 * chunkSize;
            if (dx * dx + dz * dz <= maxDist * maxDist) {
                group.visible = true;
                visibleKeys.add(key);
            }
        }
        chunks.set(key, group);
        return group;
    }

    function boundsOf(obj) {
        box.makeEmpty();
        try {
            if (obj.isInstancedMesh) {
                obj.computeBoundingBox?.();
                obj.computeBoundingSphere?.();
            }
            obj.updateMatrixWorld(true);
            box.setFromObject(obj);
        } catch {
            return null;
        }
        if (box.isEmpty()) return null;
        box.getSize(size);
        box.getCenter(center);
        return {
            minX: box.min.x, maxX: box.max.x, minZ: box.min.z, maxZ: box.max.z,
            width: size.x, depth: size.z, centerX: center.x, centerZ: center.z,
        };
    }

    function shouldStayGlobal(obj, b) {
        if (!b) return true;
        if (dynamicRoots.has(obj) || obj.isLight || obj.isCamera || obj.userData?.noSpatialChunk) return true;
        return b.width > maxChunkSpan || b.depth > maxChunkSpan;
    }

    function placeObject(obj, isLate = false) {
        if (!obj || obj.userData?.__perfChunkGroup || obj.userData?.worldChunkRoot) return false;
        const b = boundsOf(obj);
        if (shouldStayGlobal(obj, b)) {
            if (isLate && !dynamicRoots.has(obj)) freezeObject(obj);
            return false;
        }
        const ix = Math.floor(b.centerX / chunkSize);
        const iz = Math.floor(b.centerZ / chunkSize);
        const group = getChunk(ix, iz);
        group.add(obj);  
        if (isLate) {
            freezeObject(obj);
            lateObjects++;
            if (visibleKeys.has(chunkKey(ix, iz))) group.visible = true;
        }
        return true;
    }

     
    for (const obj of [...scene.children]) {
        if (!obj.userData?.worldChunkRoot) placeObject(obj, false);
    }

    let batchStats = { sourceMeshes: __qp73, mergedMeshes: __qp74, drawCallsSaved: __qp75, emptyGroupsPruned: __qp76 };
    for (const group of chunks.values()) {
        const s = batchChunkMeshes(THREE, group, dynamicMaterials, mergeMinMeshes, mergeMaxVertices);
        batchStats.sourceMeshes += s.sourceMeshes;
        batchStats.mergedMeshes += s.mergedMeshes;
        batchStats.drawCallsSaved += s.drawCallsSaved;
        batchStats.emptyGroupsPruned += pruneEmptyGroups(group);
    }

     
     
     
     
    for (const obj of scene.children) {
        if (!obj.userData?.worldChunkRoot && !dynamicRoots.has(obj)) freezeObject(obj);
    }
    scene.updateMatrixWorld(true);
    if ('matrixWorldAutoUpdate' in scene) scene.matrixWorldAutoUpdate = false;
    enabled = true;

    let lastCx = Infinity, lastCz = Infinity;
    function updateVisibility(force = false) {
        if (!enabled) return;
        const cx = Math.floor(camera.position.x / chunkSize);
        const cz = Math.floor(camera.position.z / chunkSize);
        if (!force && cx === lastCx && cz === lastCz) return;
        lastCx = cx; lastCz = cz;

        const range = Math.ceil((activeDrawDistance + Math.SQRT2 * chunkSize) / chunkSize);
        const next = new Set();
        const maxDist = activeDrawDistance + Math.SQRT2 * chunkSize;
        const maxDistSq = maxDist * maxDist;
        for (let dx = -range; dx <= range; dx++) {
            for (let dz = -range; dz <= range; dz++) {
                const key = chunkKey(cx + dx, cz + dz);
                const group = chunks.get(key);
                if (!group) continue;
                const centerX = (cx + dx + __qp77) * chunkSize;
                const centerZ = (cz + dz + __qp78) * chunkSize;
                const ddx = centerX - camera.position.x, ddz = centerZ - camera.position.z;
                if (ddx * ddx + ddz * ddz > maxDistSq) continue;
                next.add(key);
                if (!visibleKeys.has(key)) group.visible = true;
            }
        }
        for (const key of visibleKeys) {
            if (!next.has(key)) {
                const group = chunks.get(key);
                if (group) group.visible = false;
            }
        }
        visibleKeys.clear();
        for (const key of next) visibleKeys.add(key);
    }

    updateVisibility(true);

    return {
        updateVisibility,
        setDrawDistance(value) {
            const next = Number(value);
            if (!Number.isFinite(next) || next <= __qp79 || Math.abs(next - activeDrawDistance) < __qp80) return;
            activeDrawDistance = next;
            updateVisibility(true);
        },
        registerLateObject(obj) {
            if (!enabled || !obj || obj.userData?.__perfChunkGroup || obj.userData?.worldChunkRoot) return;
            placeObject(obj, true);
        },
        markDirtyObject(obj) {
            if (!obj || obj.userData?.worldChunkRoot) return false;
            // A staged shader leaf is normally already owned by a perf chunk.
            // Mark that owner directly without reparenting or recomputing the
            // entire scene. Fall back to its bounds only for unusual globals.
            let owner = obj.parent;
            while (owner && !owner.userData?.__perfChunkGroup) owner = owner.parent;
            if (owner?.userData?.__perfChunkGroup) {
                dirtyChunks.add(chunkKey(owner.userData.chunkX, owner.userData.chunkZ));
                return true;
            }
            const b = boundsOf(obj);
            if (shouldStayGlobal(obj, b)) return false;
            dirtyChunks.add(chunkKey(Math.floor(b.centerX / chunkSize), Math.floor(b.centerZ / chunkSize)));
            return true;
        },
        updateDynamicObject(obj) {
            if (!obj) return;
            obj.updateMatrix();
            obj.updateMatrixWorld(true);
        },
        getStats() {
            return {
                chunkSize,
                drawDistance: activeDrawDistance,
                chunks: chunks.size,
                visibleChunks: visibleKeys.size,
                lateObjects,
                materialReplacements: materialStats.replacements,
                uniqueMaterials: materialStats.uniqueMaterials,
                ...batchStats,
            };
        },
    };
}
