export const CODE_LORE_PRIORITY_LOAD_QUEUE = Object.freeze([
    "CODELORE|priority-load-queue.js|0001|Small bounded priority queue for non-structural network/decode work.",
    "CODELORE|priority-load-queue.js|0002|This queue is intentionally generic: streamed world structure never depends",
    "CODELORE|priority-load-queue.js|0003|on it. Callers can enqueue decorative GLTF/data work while the queue is",
    "CODELORE|priority-load-queue.js|0004|paused, then release it after the nearby structural world is READY."
]);
