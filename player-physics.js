
import { parameterNumber, registerLiteralScope } from "./numeric-parameters.js";
 
let __qp0=parameterNumber("n.physics.ed10b2c866cd",1e-4,false,"physics",0);
let __qp1=parameterNumber("n.physics.11c748c201b3",0.015,false,"physics",1);
let __qp2=parameterNumber("n.physics.108590dba63e",0,true,"physics",2);
let __qp3=parameterNumber("n.physics.df592f3f6185",0,true,"physics",3);
let __qp4=parameterNumber("n.physics.7a8e2883a6fd",0,true,"physics",4);
let __qp5=parameterNumber("n.physics.bfcf7bb8293c",0,true,"physics",5);
let __qp6=parameterNumber("n.physics.55c0214a223d",1,true,"physics",6);
let __qp7=parameterNumber("n.physics.cbd029407e92",1.65,true,"physics",7);
let __qp8=parameterNumber("n.physics.dbe6795a59b1",0.15,true,"physics",8);
let __qp9=parameterNumber("n.physics.e9e572f03b5e",0.22,true,"physics",9);
let __qp10=parameterNumber("n.physics.28bb509e5934",0.12,true,"physics",10);
let __qp11=parameterNumber("n.physics.10081f95f354",0.65,true,"physics",11);
let __qp12=parameterNumber("n.physics.25ee39e512a9",0.5,true,"physics",12);
let __qp13=parameterNumber("n.physics.7ea396bedf2b",5.5,true,"physics",13);
let __qp14=parameterNumber("n.physics.a756879d3671",-16,true,"physics",14);
let __qp15=parameterNumber("n.physics.ba9099d63079",30,true,"physics",15);
let __qp16=parameterNumber("n.physics.58f49fb5703b",0.12,true,"physics",16);
let __qp17=parameterNumber("n.physics.c675f179a074",0.15,true,"physics",17);
let __qp18=parameterNumber("n.physics.764822253b8d",1,true,"physics",18);
let __qp19=parameterNumber("n.physics.73d6cc09b34d",90,true,"physics",19);
let __qp20=parameterNumber("n.physics.65961ce640fc",0.07,true,"physics",20);
let __qp21=parameterNumber("n.physics.540ea0a9ece5",0.45,true,"physics",21);
let __qp22=parameterNumber("n.physics.fdc46d3b579f",0.12,true,"physics",22);
let __qp23=parameterNumber("n.physics.132e0cfff2ad",32,true,"physics",23);
let __qp24=parameterNumber("n.physics.148606f85a3f",2.4,true,"physics",24);
let __qp25=parameterNumber("n.physics.91b4bf52cf1e",0,true,"physics",25);
let __qp26=parameterNumber("n.physics.eecaafb52c7f",0,true,"physics",26);
let __qp27=parameterNumber("n.physics.bff5edb9d6e6",4,true,"physics",27);
let __qp28=parameterNumber("n.physics.29a394cdbc04",0.5,true,"physics",28);
let __qp29=parameterNumber("n.physics.963eca7107c8",0.05,true,"physics",29);
let __qp30=parameterNumber("n.physics.81170d000db3",0,true,"physics",30);
let __qp31=parameterNumber("n.physics.1f4a5da3b9e8",1,true,"physics",31);
let __qp32=parameterNumber("n.physics.04a249b3a904",0,true,"physics",32);
let __qp33=parameterNumber("n.physics.3f7ef4800ad6",0,true,"physics",33);
let __qp34=parameterNumber("n.physics.181cdd5cff73",0.08,true,"physics",34);
let __qp35=parameterNumber("n.physics.ead9fdac4003",0,true,"physics",35);
let __qp36=parameterNumber("n.physics.75fd80365a95",0,true,"physics",36);
let __qp37=parameterNumber("n.physics.288c6e890a54",0,true,"physics",37);
let __qp38=parameterNumber("n.physics.215af41a8daf",1,true,"physics",38);
let __qp39=parameterNumber("n.physics.a0d745a2f1bb",0,true,"physics",39);
let __qp40=parameterNumber("n.physics.118da6ed152e",0,true,"physics",40);
let __qp41=parameterNumber("n.physics.6f183a909359",-1,true,"physics",41);
let __qp42=parameterNumber("n.physics.f5296aa08164",1,true,"physics",42);
let __qp43=parameterNumber("n.physics.482097791a07",-1,true,"physics",43);
let __qp44=parameterNumber("n.physics.98e2eca184fb",1,true,"physics",44);
let __qp45=parameterNumber("n.physics.55f901f2e5ec",0.5,true,"physics",45);
let __qp46=parameterNumber("n.physics.7099c8ab4d2c",2,true,"physics",46);
let __qp47=parameterNumber("n.physics.e5163e9ef473",0,true,"physics",47);
let __qp48=parameterNumber("n.physics.317eea9280c4",0.12,true,"physics",48);
let __qp49=parameterNumber("n.physics.8dabe16306bb",0.6,true,"physics",49);
let __qp50=parameterNumber("n.physics.39cf7ae1d177",2.5,true,"physics",50);
let __qp51=parameterNumber("n.physics.782a18169936",16,true,"physics",51);
let __qp52=parameterNumber("n.physics.f8faa2504f5a",0,true,"physics",52);
let __qp53=parameterNumber("n.physics.5a2078e22a75",2,true,"physics",53);
let __qp54=parameterNumber("n.physics.792787f223a2",0,true,"physics",54);
let __qp55=parameterNumber("n.physics.9c6881d2af0a",0,true,"physics",55);
let __qp56=parameterNumber("n.physics.743e2fc9d016",0,true,"physics",56);
let __qp57=parameterNumber("n.physics.a531a66b4702",0,true,"physics",57);
let __qp58=parameterNumber("n.physics.ff604455e166",0,true,"physics",58);
let __qp59=parameterNumber("n.physics.d772a3ebcc4c",0,true,"physics",59);
let __qp60=parameterNumber("n.physics.5f42d8a1501c",0,true,"physics",60);
let __qp61=parameterNumber("n.physics.2dea98e10b39",0,true,"physics",61);
let __qp62=parameterNumber("n.physics.fbda13e5c5e3",0,true,"physics",62);
let __qp63=parameterNumber("n.physics.246eff632bd0",0,true,"physics",63);
let __qp64=parameterNumber("n.physics.f59e4c24daf3",0,true,"physics",64);
let __qp65=parameterNumber("n.physics.1238e0f2363b",0,true,"physics",65);
let __qp66=parameterNumber("n.physics.ca90e0640f39",0,true,"physics",66);
let __qp67=parameterNumber("n.physics.f1fe114cf676",0,true,"physics",67);
let __qp68=parameterNumber("n.physics.d61138171ebe",0,true,"physics",68);
let __qp69=parameterNumber("n.physics.bf322358751d",0,true,"physics",69);
let __qp70=parameterNumber("n.physics.658107434eea",1,true,"physics",70);
let __qp71=parameterNumber("n.physics.d0d79f40e64b",1,true,"physics",71);
let __qp72=parameterNumber("n.physics.082ad6b244c0",0,true,"physics",72);
let __qp73=parameterNumber("n.physics.04300c982ece",0,true,"physics",73);
let __qp74=parameterNumber("n.physics.8ff2f62fcfda",0.08,true,"physics",74);
let __qp75=parameterNumber("n.physics.b6f5bc52132b",0,true,"physics",75);
let __qp76=parameterNumber("n.physics.3ceba5b37956",0.08,true,"physics",76);
let __qp77=parameterNumber("n.physics.268043155f74",0,true,"physics",77);
let __qp78=parameterNumber("n.physics.1bf2e7d96f10",0,true,"physics",78);
let __qp79=parameterNumber("n.physics.6dd3e567801f",0,true,"physics",79);
function __setQuantitativeLiteral(index,value){switch(index){case 2:__qp2=value;return true;case 3:__qp3=value;return true;case 4:__qp4=value;return true;case 5:__qp5=value;return true;case 6:__qp6=value;return true;case 7:__qp7=value;return true;case 8:__qp8=value;return true;case 9:__qp9=value;return true;case 10:__qp10=value;return true;case 11:__qp11=value;return true;case 12:__qp12=value;return true;case 13:__qp13=value;return true;case 14:__qp14=value;return true;case 15:__qp15=value;return true;case 16:__qp16=value;return true;case 17:__qp17=value;return true;case 18:__qp18=value;return true;case 19:__qp19=value;return true;case 20:__qp20=value;return true;case 21:__qp21=value;return true;case 22:__qp22=value;return true;case 23:__qp23=value;return true;case 24:__qp24=value;return true;case 25:__qp25=value;return true;case 26:__qp26=value;return true;case 27:__qp27=value;return true;case 28:__qp28=value;return true;case 29:__qp29=value;return true;case 30:__qp30=value;return true;case 31:__qp31=value;return true;case 32:__qp32=value;return true;case 33:__qp33=value;return true;case 34:__qp34=value;return true;case 35:__qp35=value;return true;case 36:__qp36=value;return true;case 37:__qp37=value;return true;case 38:__qp38=value;return true;case 39:__qp39=value;return true;case 40:__qp40=value;return true;case 41:__qp41=value;return true;case 42:__qp42=value;return true;case 43:__qp43=value;return true;case 44:__qp44=value;return true;case 45:__qp45=value;return true;case 46:__qp46=value;return true;case 47:__qp47=value;return true;case 48:__qp48=value;return true;case 49:__qp49=value;return true;case 50:__qp50=value;return true;case 51:__qp51=value;return true;case 52:__qp52=value;return true;case 53:__qp53=value;return true;case 54:__qp54=value;return true;case 55:__qp55=value;return true;case 56:__qp56=value;return true;case 57:__qp57=value;return true;case 58:__qp58=value;return true;case 59:__qp59=value;return true;case 60:__qp60=value;return true;case 61:__qp61=value;return true;case 62:__qp62=value;return true;case 63:__qp63=value;return true;case 64:__qp64=value;return true;case 65:__qp65=value;return true;case 66:__qp66=value;return true;case 67:__qp67=value;return true;case 68:__qp68=value;return true;case 69:__qp69=value;return true;case 70:__qp70=value;return true;case 71:__qp71=value;return true;case 72:__qp72=value;return true;case 73:__qp73=value;return true;case 74:__qp74=value;return true;case 75:__qp75=value;return true;case 76:__qp76=value;return true;case 77:__qp77=value;return true;case 78:__qp78=value;return true;case 79:__qp79=value;return true;default:return false;}}
registerLiteralScope("physics",__setQuantitativeLiteral);
 
 
 
 
 
 
 
 
 
 
 
 

const EPS = __qp0;
const CONTACT_EPS = __qp1;

function clamp(v, lo, hi) {
    return Math.max(lo, Math.min(hi, v));
}

function rectContains(p, x, z, margin = __qp2) {
    return Math.abs(x - p.x) <= p.hx + margin && Math.abs(z - p.z) <= p.hz + margin;
}

function circleContains(p, x, z, margin = __qp3) {
    const dx = x - p.x;
    const dz = z - p.z;
    const r = p.radius + margin;
    return dx * dx + dz * dz <= r * r;
}

function verticalOverlap(a0, a1, b0, b1) {
    return a0 < b1 - CONTACT_EPS && a1 > b0 + CONTACT_EPS;
}

function distanceSqToSegment(x, z, seg) {
    const sx = seg.x2 - seg.x1;
    const sz = seg.z2 - seg.z1;
    const len2 = sx * sx + sz * sz;
    let t = len2 > EPS ? ((x - seg.x1) * sx + (z - seg.z1) * sz) / len2 : __qp4;
    t = clamp(t, __qp5, __qp6);
    const cx = seg.x1 + sx * t;
    const cz = seg.z1 + sz * t;
    const dx = x - cx;
    const dz = z - cz;
    return dx * dx + dz * dz;
}

function candidateScore(startX, startZ, candidate, dx, dz) {
    const movedX = candidate.x - startX;
    const movedZ = candidate.z - startZ;
    return movedX * dx + movedZ * dz;
}

export function createPlayerPhysics(options) {
    const {
        position,
        worldToCell,
        grid,
        buildingWallSegments,
        mazeSealWalls,
        propColliders,
        elevatedPlatforms,
        rampRuns,
        overheadCeilings,
        isWorldPositionAvailable = null,
    } = options;

    const eyeHeight = options.eyeHeight ?? __qp7;
    const headClearance = options.headClearance ?? __qp8;
    const bodyHeight = options.bodyHeight ?? (eyeHeight + headClearance);
    const playerRadius = options.playerRadius ?? __qp9;
    const wallThickness = options.wallThickness ?? __qp10;
    const maxStepHeight = options.maxStepHeight ?? __qp11;
    const stepDownTolerance = options.stepDownTolerance ?? __qp12;
    const jumpSpeed = options.jumpSpeed ?? __qp13;
    const gravity = options.gravity ?? __qp14;
    const maxFallSpeed = options.maxFallSpeed ?? __qp15;
    const coyoteTime = options.coyoteTime ?? __qp16;
    const jumpBufferTime = options.jumpBufferTime ?? __qp17;
    const supportMargin = options.supportMargin ?? playerRadius;
    const maxSubstepSeconds = options.maxSubstepSeconds ?? (__qp18 / __qp19);
    const maxHorizontalSubstep = options.maxHorizontalSubstep ?? Math.max(__qp20, playerRadius * __qp21);
    const maxVerticalSubstep = options.maxVerticalSubstep ?? __qp22;
    const maxSubsteps = options.maxSubsteps ?? __qp23;
    const ladderClimbSpeed = options.ladderClimbSpeed ?? __qp24;
    const boundsHalf = options.boundsHalf ?? Infinity;

    let feetY = position.y - eyeHeight;
    let verticalVelocity = __qp25;
    let grounded = true;
    let coyoteTimer = coyoteTime;
    let jumpBufferTimer = __qp26;

    let lastSafe = null;

     
     
     
     
    const spatialCellSize = options.spatialCellSize ?? __qp27;

    function spatialBucket(index, ix, iz, create = false) {
        let column = index.get(ix);
        if (!column) {
            if (!create) return null;
            index.set(ix, column = new Map());
        }
        let bucket = column.get(iz);
        if (!bucket && create) column.set(iz, bucket = []);
        return bucket || null;
    }

    function insertSpatialItem(map, item, boundsFor) {
        const b = boundsFor(item);
        const ix0 = Math.floor(b.minX / spatialCellSize);
        const ix1 = Math.floor(b.maxX / spatialCellSize);
        const iz0 = Math.floor(b.minZ / spatialCellSize);
        const iz1 = Math.floor(b.maxZ / spatialCellSize);
        for (let ix = ix0; ix <= ix1; ix++) {
            for (let iz = iz0; iz <= iz1; iz++) spatialBucket(map, ix, iz, true).push(item);
        }
    }

    function buildSpatialIndex(items, boundsFor) {
         
         
         
         
        const map = new Map();
        for (const item of items) insertSpatialItem(map, item, boundsFor);
        return map;
    }

    function querySpatial(index, x, z) {
        return spatialBucket(index, Math.floor(x / spatialCellSize), Math.floor(z / spatialCellSize)) ?? EMPTY_BUCKET;
    }

    const EMPTY_BUCKET = Object.freeze([]);
    const physicsItemActive = item => item && item.__physicsDisabled !== true;
    const platformBounds = p => ({
        minX: p.x - p.hx - playerRadius, maxX: p.x + p.hx + playerRadius,
        minZ: p.z - p.hz - playerRadius, maxZ: p.z + p.hz + playerRadius,
    });
    const ceilingBounds = c => ({
        minX: c.x - c.hx - playerRadius, maxX: c.x + c.hx + playerRadius,
        minZ: c.z - c.hz - playerRadius, maxZ: c.z + c.hz + playerRadius,
    });
    const rampBounds = r => {
        if (r.axis === 'x') return {
            minX: Math.min(r.from, r.to) - playerRadius, maxX: Math.max(r.from, r.to) + playerRadius,
            minZ: r.fixedCoord - r.halfWidth - playerRadius, maxZ: r.fixedCoord + r.halfWidth + playerRadius,
        };
        return {
            minX: r.fixedCoord - r.halfWidth - playerRadius, maxX: r.fixedCoord + r.halfWidth + playerRadius,
            minZ: Math.min(r.from, r.to) - playerRadius, maxZ: Math.max(r.from, r.to) + playerRadius,
        };
    };
    const propBounds = p => {
        const r = p.radius + playerRadius;
        return { minX: p.x - r, maxX: p.x + r, minZ: p.z - r, maxZ: p.z + r };
    };
    const mazeBounds = seg => {
        const r = playerRadius + wallThickness * __qp28 + __qp29;
        return {
            minX: Math.min(seg.x1, seg.x2) - r, maxX: Math.max(seg.x1, seg.x2) + r,
            minZ: Math.min(seg.z1, seg.z2) - r, maxZ: Math.max(seg.z1, seg.z2) + r,
        };
    };
    let platformIndex = buildSpatialIndex(elevatedPlatforms, platformBounds);
    let ceilingIndex = buildSpatialIndex(overheadCeilings, ceilingBounds);
    let rampIndex = buildSpatialIndex(rampRuns, rampBounds);
    let propIndex = buildSpatialIndex(propColliders, propBounds);
    let mazeIndex = buildSpatialIndex(mazeSealWalls, mazeBounds);
    let indexedPlatformCount = elevatedPlatforms.length;
    let indexedCeilingCount = overheadCeilings.length;
    let indexedRampCount = rampRuns.length;
    let indexedPropCount = propColliders.length;
    let indexedMazeCount = mazeSealWalls.length;

    function platformSupportsAt(p, x, z) {
        return rectContains(p, x, z, supportMargin);
    }

    function rampHeightAt(r, x, z, margin = supportMargin) {
        const along = r.axis === 'x' ? x : z;
        const cross = r.axis === 'x' ? z : x;
        if (Math.abs(cross - r.fixedCoord) > r.halfWidth + margin) return null;

        const lo = Math.min(r.from, r.to) - margin;
        const hi = Math.max(r.from, r.to) + margin;
        if (along < lo || along > hi) return null;

        const denom = r.to - r.from;
        if (Math.abs(denom) < EPS) return Math.max(r.y0, r.y1);
        const t = clamp((along - r.from) / denom, __qp30, __qp31);
        return r.y0 + (r.y1 - r.y0) * t;
    }

    function supportAt(x, z, currentFeetY) {
        let best = { y: __qp32, kind: 'ground' };
        const maxY = currentFeetY + maxStepHeight + CONTACT_EPS;

        const consider = (y, kind) => {
            if (y > maxY) return;
            if (y > best.y + CONTACT_EPS) {
                best = { y, kind };
                return;
            }
             
             
             
            if (Math.abs(y - best.y) <= CONTACT_EPS && best.kind === 'ladder' && kind !== 'ladder') {
                best = { y, kind };
            }
        };

        for (const p of querySpatial(platformIndex, x, z)) {
            if (!physicsItemActive(p) || !platformSupportsAt(p, x, z)) continue;
            consider(p.y, p.supportKind ?? 'platform');
        }

        for (const r of querySpatial(rampIndex, x, z)) {
            if (!physicsItemActive(r)) continue;
            const y = rampHeightAt(r, x, z);
            if (y !== null) consider(y, 'ramp');
        }

        for (const p of querySpatial(propIndex, x, z)) {
            if (!physicsItemActive(p) || p.height === Infinity || !circleContains(p, x, z, supportMargin)) continue;
            consider(p.height, 'prop');
        }

        return best;
    }

    function supportHeightAt(x, z, currentFeetY) {
        return supportAt(x, z, currentFeetY).y;
    }

    function allSurfaceHeightsAt(x, z) {
        const ys = [__qp33];

        for (const p of querySpatial(platformIndex, x, z)) {
            if (physicsItemActive(p) && platformSupportsAt(p, x, z)) ys.push(p.y);
        }

        for (const r of querySpatial(rampIndex, x, z)) {
            if (!physicsItemActive(r)) continue;
            const y = rampHeightAt(r, x, z);
            if (y !== null) ys.push(y);
        }

        for (const p of querySpatial(propIndex, x, z)) {
            if (!physicsItemActive(p) || p.height === Infinity) continue;
            if (circleContains(p, x, z, supportMargin)) ys.push(p.height);
        }

        return ys;
    }

    function restingSupportHeightAt(x, z, currentFeetY, tolerance = __qp34) {
        let best = __qp35;
        for (const y of allSurfaceHeightsAt(x, z)) {
            if (y <= currentFeetY + tolerance && y > best) best = y;
        }
        return best;
    }

    function isCeilingSolid(p) {
         
         
        return p.blocksFromBelow !== false;
    }

    function nearestCeilingAbove(x, z, fromFeetY) {
        let nearest = Infinity;

        for (const p of querySpatial(platformIndex, x, z)) {
            if (!physicsItemActive(p) || !isCeilingSolid(p) || p.y <= fromFeetY + CONTACT_EPS) continue;
            if (rectContains(p, x, z, playerRadius) && p.y < nearest) nearest = p.y;
        }

        for (const c of querySpatial(ceilingIndex, x, z)) {
            if (!physicsItemActive(c) || c.y <= fromFeetY + CONTACT_EPS) continue;
            if (rectContains(c, x, z, playerRadius) && c.y < nearest) nearest = c.y;
        }

        return nearest;
    }

    function hasStandingClearance(x, z, atFeetY) {
        const ceiling = nearestCeilingAbove(x, z, atFeetY);
        return ceiling === Infinity || ceiling - atFeetY >= bodyHeight - CONTACT_EPS;
    }

     
     
     
    const buildingWallGrid = Array.from({ length: grid.length }, () => []);
    const indexedBuildingWallKeys = new Set();
    function indexBuildingWallEntry(key, walls) {
        const comma = key.indexOf(',');
        if (comma < __qp36) return;
        const row = Number(key.slice(__qp37, comma));
        const col = Number(key.slice(comma + __qp38));
        if (Number.isInteger(row) && Number.isInteger(col) && row >= __qp39 && row < buildingWallGrid.length) {
            buildingWallGrid[row][col] = walls;
            indexedBuildingWallKeys.add(key);
        }
    }
    for (const [key, walls] of buildingWallSegments) indexBuildingWallEntry(key, walls);

     
     
     
     
     
    function syncDynamicWorld() {
        const before = {
            platforms: indexedPlatformCount, ceilings: indexedCeilingCount,
            ramps: indexedRampCount, props: indexedPropCount, maze: indexedMazeCount,
        };
        while (indexedPlatformCount < elevatedPlatforms.length) insertSpatialItem(platformIndex, elevatedPlatforms[indexedPlatformCount++], platformBounds);
        while (indexedCeilingCount < overheadCeilings.length) insertSpatialItem(ceilingIndex, overheadCeilings[indexedCeilingCount++], ceilingBounds);
        while (indexedRampCount < rampRuns.length) insertSpatialItem(rampIndex, rampRuns[indexedRampCount++], rampBounds);
        while (indexedPropCount < propColliders.length) insertSpatialItem(propIndex, propColliders[indexedPropCount++], propBounds);
        while (indexedMazeCount < mazeSealWalls.length) insertSpatialItem(mazeIndex, mazeSealWalls[indexedMazeCount++], mazeBounds);
        for (const [key, walls] of buildingWallSegments) if (!indexedBuildingWallKeys.has(key)) indexBuildingWallEntry(key, walls);
        return {
            addedPlatforms: indexedPlatformCount - before.platforms,
            addedCeilings: indexedCeilingCount - before.ceilings,
            addedRamps: indexedRampCount - before.ramps,
            addedProps: indexedPropCount - before.props,
            addedMazeWalls: indexedMazeCount - before.maze,
        };
    }
     
     
     
     
    // SANITY HANDOFF: already-valid player occupancy outranks late procedural
    // geometry. P1 staged conflicting collision per primitive; P3 closes the authority
    // gap by staging the WHOLE owner when any primitive intersects the current capsule.
    // That lets the render owner remain hidden too: no invisible sibling collision, no
    // visible-but-non-solid structure, and no routine rollback to historical lastSafe.
    const ownedWorld = new Map();
    const ownedCompactionThreshold = Math.max(8, options.ownedCompactionThreshold ?? 48);
    let lastDeferredRetryX = Infinity;
    let lastDeferredRetryZ = Infinity;
    let lastDeferredRetryFeetY = Infinity;

    function normalizedOwnedData(data = {}) {
        return {
            platforms: [...(data.platforms || [])],
            ceilings: [...(data.ceilings || [])],
            ramps: [...(data.ramps || [])],
            props: [...(data.props || [])],
            mazeWalls: [...(data.mazeWalls || [])],
        };
    }

    function ownedItems(data) {
        return [
            ...data.platforms, ...data.ceilings, ...data.ramps,
            ...data.props, ...data.mazeWalls,
        ];
    }

    function ownedEntries(data) {
        return [
            ...data.platforms.map(item => ({ kind: 'platforms', item, indexed: false, activationState: 'staged' })),
            ...data.ceilings.map(item => ({ kind: 'ceilings', item, indexed: false, activationState: 'staged' })),
            ...data.ramps.map(item => ({ kind: 'ramps', item, indexed: false, activationState: 'staged' })),
            ...data.props.map(item => ({ kind: 'props', item, indexed: false, activationState: 'staged' })),
            ...data.mazeWalls.map(item => ({ kind: 'mazeWalls', item, indexed: false, activationState: 'staged' })),
        ];
    }

    function ownedIndexForKind(kind) {
        if (kind === 'platforms') return platformIndex;
        if (kind === 'ceilings') return ceilingIndex;
        if (kind === 'ramps') return rampIndex;
        if (kind === 'props') return propIndex;
        if (kind === 'mazeWalls') return mazeIndex;
        throw new Error(`unknown owned physics kind: ${kind}`);
    }

    function ownedBoundsForKind(kind) {
        if (kind === 'platforms') return platformBounds;
        if (kind === 'ceilings') return ceilingBounds;
        if (kind === 'ramps') return rampBounds;
        if (kind === 'props') return propBounds;
        if (kind === 'mazeWalls') return mazeBounds;
        throw new Error(`unknown owned physics kind: ${kind}`);
    }

    function ownedEntryOverlapsCurrentCapsule(entry) {
        const item = entry.item;
        const x = position.x;
        const z = position.z;
        const bodyBottom = feetY;
        const bodyTop = feetY + bodyHeight;

        if (entry.kind === 'platforms' || entry.kind === 'ceilings') {
            if (!rectContains(item, x, z, playerRadius)) return false;
            if (!Number.isFinite(item.y)) return true;
            return item.y > bodyBottom + CONTACT_EPS && item.y < bodyTop - CONTACT_EPS;
        }

        if (entry.kind === 'ramps') {
            const y = rampHeightAt(item, x, z, playerRadius);
            if (y === null) return false;
            if (!Number.isFinite(y)) return true;
            return y > bodyBottom + CONTACT_EPS && y < bodyTop - CONTACT_EPS;
        }

        if (entry.kind === 'props') {
            const minDist = item.radius + playerRadius;
            const dx = x - item.x;
            const dz = z - item.z;
            if (dx * dx + dz * dz >= minDist * minDist) return false;
            const yMin = item.yMin ?? 0;
            const yMax = item.height ?? Infinity;
            return verticalOverlap(bodyBottom, bodyTop, yMin, yMax);
        }

        if (entry.kind === 'mazeWalls') {
            const yMin = item.yMin ?? -Infinity;
            const yMax = item.yMax ?? Infinity;
            if (!verticalOverlap(bodyBottom, bodyTop, yMin, yMax)) return false;
            const minWallDistSq = (playerRadius + wallThickness * __qp45) ** __qp46;
            return distanceSqToSegment(x, z, item) < minWallDistSq;
        }

        return false;
    }

    function activateOwnedEntry(record, entry) {
        const item = entry.item;
        item.__physicsOwner = record.ownerId;
        item.__physicsDisabled = false;
        if (!entry.indexed) {
            insertSpatialItem(ownedIndexForKind(entry.kind), item, ownedBoundsForKind(entry.kind));
            entry.indexed = true;
        }
        entry.activationState = 'active';
    }

    function deferOwnedEntry(record, entry) {
        entry.item.__physicsOwner = record.ownerId;
        entry.item.__physicsDisabled = true;
        entry.activationState = 'deferred-player-overlap';
    }

    function notifyOwnedActivation(record, previousState) {
        if (record.activationState === previousState || typeof record.onActivationChange !== 'function') return;
        try { record.onActivationChange(record); }
        catch (error) { console.warn(`[physics] owned-world activation listener failed for ${record.ownerId}`, error); }
    }

    function ownedRecordOverlapsCurrentCapsule(record) {
        return record.entries.some(ownedEntryOverlapsCurrentCapsule);
    }

    function deferOwnedRecord(record) {
        const previousState = record.activationState;
        for (const entry of record.entries) deferOwnedEntry(record, entry);
        record.activationState = 'deferred-player-overlap';
        record.deferredReason = 'player-capsule-overlap';
        notifyOwnedActivation(record, previousState);
    }

    function activateOwnedRecord(record) {
        const previousState = record.activationState;
        for (const entry of record.entries) activateOwnedEntry(record, entry);
        record.activationState = 'active';
        record.deferredReason = null;
        notifyOwnedActivation(record, previousState);
    }

    function stageOwnedRecord(record) {
        record.active = true;
        if (ownedRecordOverlapsCurrentCapsule(record)) deferOwnedRecord(record);
        else activateOwnedRecord(record);
        const deferredItems = record.activationState === 'deferred-player-overlap' ? record.entries.length : 0;
        return { deferredItems, activationState: record.activationState };
    }

    function indexOwnedRecord(record) {
        for (const entry of record.entries) {
            entry.indexed = false;
            if (record.active && entry.activationState === 'active' && entry.item.__physicsDisabled !== true) {
                activateOwnedEntry(record, entry);
            }
        }
    }

    function compactOwnedWorld() {
        let removedOwners = 0, removedItems = 0;
        for (const [ownerId, record] of ownedWorld) {
            if (record.active) continue;
            removedOwners++;
            removedItems += record.items.length;
            ownedWorld.delete(ownerId);
        }
        if (!removedOwners) return { removedOwners: 0, removedItems: 0, ...ownedWorldStats() };

        platformIndex = buildSpatialIndex(elevatedPlatforms, platformBounds);
        ceilingIndex = buildSpatialIndex(overheadCeilings, ceilingBounds);
        rampIndex = buildSpatialIndex(rampRuns, rampBounds);
        propIndex = buildSpatialIndex(propColliders, propBounds);
        mazeIndex = buildSpatialIndex(mazeSealWalls, mazeBounds);
        indexedPlatformCount = elevatedPlatforms.length;
        indexedCeilingCount = overheadCeilings.length;
        indexedRampCount = rampRuns.length;
        indexedPropCount = propColliders.length;
        indexedMazeCount = mazeSealWalls.length;

        for (const record of ownedWorld.values()) indexOwnedRecord(record);
        return { removedOwners, removedItems, ...ownedWorldStats() };
    }

    function registerOwnedWorld(ownerId, data = {}, lifecycle = {}) {
        if (ownerId === undefined || ownerId === null) throw new Error('registerOwnedWorld requires ownerId');
        const existing = ownedWorld.get(ownerId);
        if (existing) {
            if (typeof lifecycle.onActivationChange === 'function') existing.onActivationChange = lifecycle.onActivationChange;
            stageOwnedRecord(existing);
            return existing;
        }

        const ownedData = normalizedOwnedData(data);
        const record = {
            ownerId,
            data: ownedData,
            items: ownedItems(ownedData),
            entries: ownedEntries(ownedData),
            active: true,
            activationState: 'staged',
            deferredReason: null,
            onActivationChange: typeof lifecycle.onActivationChange === 'function' ? lifecycle.onActivationChange : null,
        };
        ownedWorld.set(ownerId, record);
        stageOwnedRecord(record);
        return record;
    }

    function unregisterOwnedWorld(ownerId) {
        const record = ownedWorld.get(ownerId);
        if (!record) return false;
        const previousState = record.activationState;
        for (const entry of record.entries) {
            entry.item.__physicsDisabled = true;
            entry.activationState = 'inactive';
        }
        record.active = false;
        record.activationState = 'inactive';
        record.deferredReason = null;
        notifyOwnedActivation(record, previousState);
        // Do not let an inactive collision tombstone retain its render payload through
        // the lifecycle callback while waiting for the normal compaction threshold.
        record.onActivationChange = null;

        let inactiveOwners = 0;
        for (const candidate of ownedWorld.values()) if (!candidate.active) inactiveOwners++;
        if (inactiveOwners >= ownedCompactionThreshold) compactOwnedWorld();
        return true;
    }

    function retryDeferredOwnedWorld({ force = false } = {}) {
        const before = ownedWorldStats();
        if (!before.deferredItems) return { activatedItems: 0, pendingItems: 0, ...before };

        const movedEnough = !Number.isFinite(lastDeferredRetryX)
            || Math.hypot(position.x - lastDeferredRetryX, position.z - lastDeferredRetryZ) >= playerRadius
            || Math.abs(feetY - lastDeferredRetryFeetY) >= playerRadius;
        if (!force && !movedEnough) return { activatedItems: 0, pendingItems: before.deferredItems, ...before };

        lastDeferredRetryX = position.x;
        lastDeferredRetryZ = position.z;
        lastDeferredRetryFeetY = feetY;
        let activatedItems = 0;
        let pendingItems = 0;
        for (const record of ownedWorld.values()) {
            if (!record.active || record.activationState !== 'deferred-player-overlap') continue;
            if (ownedRecordOverlapsCurrentCapsule(record)) {
                pendingItems += record.entries.length;
                continue;
            }
            activatedItems += record.entries.length;
            activateOwnedRecord(record);
        }
        return { activatedItems, pendingItems, ...ownedWorldStats() };
    }

    function ownedWorldStats() {
        let activeOwners = 0, activeItems = 0, totalItems = 0, deferredItems = 0, deferredOwners = 0;
        for (const record of ownedWorld.values()) {
            totalItems += record.items.length;
            if (!record.active) continue;
            activeOwners++;
            let ownerDeferred = false;
            for (const entry of record.entries) {
                if (entry.activationState === 'active' && entry.item.__physicsDisabled !== true) activeItems++;
                else if (entry.activationState === 'deferred-player-overlap') {
                    deferredItems++;
                    ownerDeferred = true;
                }
            }
            if (ownerDeferred) deferredOwners++;
        }
        return {
            owners: ownedWorld.size,
            activeOwners,
            inactiveOwners: ownedWorld.size - activeOwners,
            activeItems,
            totalItems,
            deferredItems,
            deferredOwners,
            compactionThreshold: ownedCompactionThreshold,
        };
    }

    const nearbyBuildingWalls = [];
    function buildingWallsNear(x, z) {
        const { col, row } = worldToCell(x, z);
        nearbyBuildingWalls.length = __qp40;
        for (let dr = __qp41; dr <= __qp42; dr++) {
            for (let dc = __qp43; dc <= __qp44; dc++) {
                const c = col + dc;
                const r = row + dr;
                if (!grid[r]?.[c]) continue;
                const walls = buildingWallGrid[r]?.[c];
                if (walls) nearbyBuildingWalls.push(walls);
            }
        }
        return nearbyBuildingWalls;
    }

    function bodyBlockedHorizontally(x, z, atFeetY) {
        if (!Number.isFinite(x) || !Number.isFinite(z) || !Number.isFinite(atFeetY)) return true;
        if (Math.abs(x) > boundsHalf || Math.abs(z) > boundsHalf) return true;
         
         
         
         
        if (typeof isWorldPositionAvailable === 'function' && !isWorldPositionAvailable(x, z)) return true;

        const bodyBottom = atFeetY;
        const bodyTop = atFeetY + bodyHeight;
        const minWallDistSq = (playerRadius + wallThickness * __qp45) ** __qp46;

        for (const walls of buildingWallsNear(x, z)) {
            for (const floor of walls.floors) {
                if (!verticalOverlap(bodyBottom, bodyTop, floor.yMin, floor.yMax)) continue;
                for (const seg of floor.segments) {
                    if (distanceSqToSegment(x, z, seg) < minWallDistSq) return true;
                }
            }
        }

        for (const seg of querySpatial(mazeIndex, x, z)) {
            if (!physicsItemActive(seg)) continue;
            const yMin = seg.yMin ?? -Infinity;
            const yMax = seg.yMax ?? Infinity;
            if (!verticalOverlap(bodyBottom, bodyTop, yMin, yMax)) continue;
            if (distanceSqToSegment(x, z, seg) < minWallDistSq) return true;
        }

        for (const p of querySpatial(propIndex, x, z)) {
            if (!physicsItemActive(p)) continue;
            const minDist = p.radius + playerRadius;
            const dx = x - p.x;
            const dz = z - p.z;
            if (dx * dx + dz * dz >= minDist * minDist) continue;

            if (p.height === Infinity) return true;
            const propBottom = p.yMin ?? __qp47;
            if (verticalOverlap(bodyBottom, bodyTop, propBottom, p.height)) return true;
        }

        return false;
    }

    function poseIsValid(x, z, atFeetY) {
        if (atFeetY < -CONTACT_EPS) return false;
        if (bodyBlockedHorizontally(x, z, atFeetY)) return false;
        if (!hasStandingClearance(x, z, atFeetY)) return false;
        return true;
    }

     
     
     
     
    function findBootstrapSafePose(originX, originZ, atFeetY) {
        if (poseIsValid(originX, originZ, atFeetY)) return { x: originX, z: originZ, feetY: atFeetY };

        const radialStep = Math.max(__qp48, playerRadius * __qp49);
        const maxRadius = options.bootstrapSearchRadius ?? __qp50;
        const spokes = __qp51;
        for (let radius = radialStep; radius <= maxRadius + EPS; radius += radialStep) {
            for (let i = __qp52; i < spokes; i++) {
                const angle = (i / spokes) * Math.PI * __qp53;
                const x = originX + Math.cos(angle) * radius;
                const z = originZ + Math.sin(angle) * radius;
                if (poseIsValid(x, z, atFeetY)) return { x, z, feetY: atFeetY };
            }
        }
        return null;
    }

    function groundedCandidate(x, z, currentFeetY, dt) {
        const support = supportAt(x, z, currentFeetY);
        const leavesGround = support.y < currentFeetY - stepDownTolerance;
        let targetFeetY = leavesGround ? currentFeetY : support.y;

         
         
         
         
        if (!leavesGround && support.kind === 'ladder' && targetFeetY > currentFeetY) {
            targetFeetY = Math.min(targetFeetY, currentFeetY + ladderClimbSpeed * dt);
        }

        if (!poseIsValid(x, z, targetFeetY)) return null;
        return { x, z, feetY: targetFeetY, leavesGround };
    }

    function airborneCandidate(x, z, currentFeetY) {
        if (!poseIsValid(x, z, currentFeetY)) return null;
        return { x, z, feetY: currentFeetY, leavesGround: true };
    }

    function chooseHorizontalMove(dx, dz, evaluator) {
        if (Math.abs(dx) < EPS && Math.abs(dz) < EPS) return evaluator(position.x, position.z);

        const startX = position.x;
        const startZ = position.z;
        const candidates = [];

        const full = evaluator(startX + dx, startZ + dz);
        if (full) return full;

        if (Math.abs(dx) >= EPS) {
            const xOnly = evaluator(startX + dx, startZ);
            if (xOnly) candidates.push(xOnly);
        }
        if (Math.abs(dz) >= EPS) {
            const zOnly = evaluator(startX, startZ + dz);
            if (zOnly) candidates.push(zOnly);
        }

        if (!candidates.length) return evaluator(startX, startZ);
        candidates.sort((a, b) => candidateScore(startX, startZ, b, dx, dz) - candidateScore(startX, startZ, a, dx, dz));
        return candidates[__qp54];
    }

    function findLandingSurface(x, z, startFeetY, predictedFeetY) {
        let landing = null;
        for (const y of allSurfaceHeightsAt(x, z)) {
            if (startFeetY < y - CONTACT_EPS || predictedFeetY > y + CONTACT_EPS) continue;
            if (!hasStandingClearance(x, z, y)) continue;
            if (bodyBlockedHorizontally(x, z, y)) continue;
            if (landing === null || y > landing) landing = y;
        }
        return landing;
    }

    function findCeilingCrossing(x, z, startFeetY, predictedFeetY) {
        const startTop = startFeetY + bodyHeight;
        const predictedTop = predictedFeetY + bodyHeight;
        if (predictedTop <= startTop) return null;

        let hit = null;
        const consider = (c, solid = true) => {
            if (!solid || !rectContains(c, x, z, playerRadius)) return;
            if (c.y < startTop - CONTACT_EPS || c.y > predictedTop + CONTACT_EPS) return;
            if (hit === null || c.y < hit) hit = c.y;
        };

        for (const p of querySpatial(platformIndex, x, z)) if (physicsItemActive(p)) consider(p, isCeilingSolid(p));
        for (const c of querySpatial(ceilingIndex, x, z)) if (physicsItemActive(c)) consider(c, true);
        return hit;
    }

    function restore(snapshot) {
        if (!snapshot) return false;
        position.x = snapshot.x;
        position.z = snapshot.z;
        feetY = snapshot.feetY;
        verticalVelocity = snapshot.verticalVelocity ?? __qp55;
        grounded = snapshot.grounded ?? false;
        position.y = feetY + eyeHeight;
        return true;
    }

    function rememberSafe() {
        if (!poseIsValid(position.x, position.z, feetY)) return;
        lastSafe = {
            x: position.x,
            z: position.z,
            feetY,
            verticalVelocity,
            grounded,
        };
    }

    function simulateSubstep(dt, wishVelocityX, wishVelocityZ) {
        const start = {
            x: position.x,
            z: position.z,
            feetY,
            verticalVelocity,
            grounded,
        };

         
         
         
        if (jumpBufferTimer > __qp56 && (grounded || coyoteTimer > __qp57)) {
            verticalVelocity = jumpSpeed;
            grounded = false;
            jumpBufferTimer = __qp58;
            coyoteTimer = __qp59;
        }

        if (grounded) coyoteTimer = coyoteTime;
        else coyoteTimer = Math.max(__qp60, coyoteTimer - dt);
        jumpBufferTimer = Math.max(__qp61, jumpBufferTimer - dt);

        const dx = wishVelocityX * dt;
        const dz = wishVelocityZ * dt;

        if (grounded) {
            const moved = chooseHorizontalMove(dx, dz, (x, z) => groundedCandidate(x, z, feetY, dt));
            if (!moved) {
                restore(start);
                return;
            }
            position.x = moved.x;
            position.z = moved.z;
            feetY = moved.feetY;
            if (moved.leavesGround) grounded = false;
        } else {
            const moved = chooseHorizontalMove(dx, dz, (x, z) => airborneCandidate(x, z, feetY));
            if (!moved) {
                restore(start);
                return;
            }
            position.x = moved.x;
            position.z = moved.z;
        }

        if (!grounded) {
            verticalVelocity = Math.max(-maxFallSpeed, verticalVelocity + gravity * dt);
            const predictedFeetY = feetY + verticalVelocity * dt;

            if (verticalVelocity > __qp62) {
                const ceilingY = findCeilingCrossing(position.x, position.z, feetY, predictedFeetY);
                if (ceilingY !== null) {
                    const bonkFeetY = ceilingY - bodyHeight;
                    if (bonkFeetY < -CONTACT_EPS || !poseIsValid(position.x, position.z, bonkFeetY)) {
                        restore(start);
                        verticalVelocity = Math.min(__qp63, verticalVelocity);
                        return;
                    }
                    feetY = bonkFeetY;
                    verticalVelocity = __qp64;
                } else {
                    feetY = predictedFeetY;
                }
            } else {
                const landing = findLandingSurface(position.x, position.z, feetY, predictedFeetY);
                if (landing !== null) {
                    feetY = landing;
                    verticalVelocity = __qp65;
                    grounded = true;
                    coyoteTimer = coyoteTime;
                } else {
                    feetY = predictedFeetY;
                }
            }
        }

        position.y = feetY + eyeHeight;

         
         
         
         
        if (!poseIsValid(position.x, position.z, feetY)) {
            restore(start);
            if (!poseIsValid(position.x, position.z, feetY) && lastSafe) {
                restore(lastSafe);
                verticalVelocity = __qp66;
            }
            return;
        }

        rememberSafe();
    }

    function step(deltaSeconds, wishVelocityX = __qp67, wishVelocityZ = __qp68) {
        if (!Number.isFinite(deltaSeconds) || deltaSeconds <= __qp69) return getState();

        const horizontalTravel = Math.hypot(wishVelocityX, wishVelocityZ) * deltaSeconds;
        const predictedEndVy = clamp(verticalVelocity + gravity * deltaSeconds, -maxFallSpeed, jumpSpeed);
        const verticalTravel = Math.max(Math.abs(verticalVelocity), Math.abs(predictedEndVy)) * deltaSeconds;

        const byTime = Math.ceil(deltaSeconds / maxSubstepSeconds);
        const byHorizontal = Math.ceil(horizontalTravel / maxHorizontalSubstep);
        const byVertical = Math.ceil(verticalTravel / maxVerticalSubstep);
        const count = clamp(Math.max(__qp70, byTime, byHorizontal, byVertical), __qp71, maxSubsteps);
        const dt = deltaSeconds / count;

        for (let i = __qp72; i < count; i++) simulateSubstep(dt, wishVelocityX, wishVelocityZ);
        position.y = feetY + eyeHeight;
        retryDeferredOwnedWorld();
        return getState();
    }

    function bufferJump() {
        jumpBufferTimer = jumpBufferTime;
    }

    function syncFromPosition({ forceAirborne = false, resetVelocity = true, allowLastSafeFallback = true } = {}) {
        feetY = position.y - eyeHeight;
        if (resetVelocity) verticalVelocity = __qp73;

        if (forceAirborne) {
            grounded = false;
        } else {
             
             
             
             
            const support = restingSupportHeightAt(position.x, position.z, feetY);
            grounded = Math.abs(support - feetY) <= __qp74 && poseIsValid(position.x, position.z, support);
            if (grounded) feetY = support;
        }

        position.y = feetY + eyeHeight;

        if (poseIsValid(position.x, position.z, feetY)) {
            rememberSafe();
        } else if (allowLastSafeFallback && lastSafe) {
            restore(lastSafe);
        } else {
            const bootstrap = findBootstrapSafePose(position.x, position.z, feetY);
            if (bootstrap) {
                position.x = bootstrap.x;
                position.z = bootstrap.z;
                feetY = bootstrap.feetY;
                verticalVelocity = __qp75;
                grounded = Math.abs(restingSupportHeightAt(position.x, position.z, feetY) - feetY) <= __qp76;
                position.y = feetY + eyeHeight;
                rememberSafe();
            }
        }

        if (resetVelocity) verticalVelocity = __qp77;
        coyoteTimer = grounded ? coyoteTime : __qp78;
        jumpBufferTimer = __qp79;
        position.y = feetY + eyeHeight;
        retryDeferredOwnedWorld({ force: true });
        return getState();
    }

    function getState() {
        return {
            feetY,
            verticalVelocity,
            grounded,
            coyoteTimer,
            jumpBufferTimer,
            lastSafe: { ...lastSafe },
        };
    }

    // SPAWN / SANITY HANDOFF: startup proof must exercise the real controller,
    // but it must not move the real player, rewrite lastSafe, or trigger deferred
    // owner activation. Probe commands therefore run through simulateSubstep and
    // restore every mutable controller field before returning.
    function captureMotionState() {
        return {
            x: position.x, z: position.z, feetY, verticalVelocity, grounded,
            coyoteTimer, jumpBufferTimer, lastSafe: lastSafe ? { ...lastSafe } : null,
        };
    }

    function restoreMotionState(snapshot) {
        position.x = snapshot.x;
        position.z = snapshot.z;
        feetY = snapshot.feetY;
        verticalVelocity = snapshot.verticalVelocity;
        grounded = snapshot.grounded;
        coyoteTimer = snapshot.coyoteTimer;
        jumpBufferTimer = snapshot.jumpBufferTimer;
        lastSafe = snapshot.lastSafe ? { ...snapshot.lastSafe } : null;
        position.y = feetY + eyeHeight;
    }

    function probeControllerPath({ start = null, steps = [] } = {}) {
        const saved = captureMotionState();
        try {
            if (start) {
                position.x = start.x;
                position.z = start.z;
                feetY = start.feetY;
                verticalVelocity = __qp73;
                grounded = Math.abs(restingSupportHeightAt(position.x, position.z, feetY) - feetY) <= __qp74
                    && poseIsValid(position.x, position.z, feetY);
                coyoteTimer = grounded ? coyoteTime : __qp78;
                jumpBufferTimer = __qp79;
                lastSafe = null;
                position.y = feetY + eyeHeight;
                if (poseIsValid(position.x, position.z, feetY)) rememberSafe();
            }

            const originX = position.x;
            const originZ = position.z;
            const originFeetY = feetY;
            if (!poseIsValid(originX, originZ, originFeetY)) {
                return { validStart: false, validEnd: false, distance: __qp25, maxDistance: __qp25, completedSteps: __qp26 };
            }

            let maxDistance = __qp25;
            let completedSteps = __qp26;
            let validEnd = true;
            for (const command of steps) {
                if (!command || !Number.isFinite(command.dt) || command.dt <= __qp69) continue;
                simulateSubstep(command.dt, command.wishVelocityX ?? __qp67, command.wishVelocityZ ?? __qp68);
                completedSteps++;
                if (!poseIsValid(position.x, position.z, feetY)) {
                    validEnd = false;
                    break;
                }
                maxDistance = Math.max(maxDistance, Math.hypot(position.x - originX, position.z - originZ));
            }

            return {
                validStart: true,
                validEnd,
                distance: Math.hypot(position.x - originX, position.z - originZ),
                maxDistance,
                completedSteps,
                end: { x: position.x, z: position.z, feetY, grounded },
            };
        } finally {
            restoreMotionState(saved);
        }
    }

     
     
     
    syncFromPosition();

    return {
        step,
        bufferJump,
        syncFromPosition,
        syncDynamicWorld,
        registerOwnedWorld,
        unregisterOwnedWorld,
        retryDeferredOwnedWorld,
        compactOwnedWorld,
        ownedWorldStats,
        getState,
        supportHeightAt,
        poseIsValid,
        probeControllerPath,
    };
}
