# JWEB visual harness — live integration status

Integrated: 2026-09-06

The previous parked state is superseded for this tree. Historical parked notes are retained only as provenance.

Current state:

- REAL CITY mode is wired to `main.js` through a lazy dev-only dynamic import.
- Normal boot does not statically import the harness.
- Live payload collection includes READY streamed chunks, authored spawn fabric, authored relationships, and the authored ceiling overlay.
- `gotoChunk()` can move freecam to a real streamed chunk and wait for its local render ring.
- `captureInvestigation()` remains the engineering/bug workflow.
- `captureArtPass()` is a first-class artistic workflow with composition, massing, circulation, facade, material, and readability lenses.
- Generator-specimen and direct-fixture modes remain independent of `main.js`.

Historical `ORIGIN-PARKED-STATUS.md` and `PARKED-TOOL-STATUS.md` describe how the tool arrived here, not its current integration state.
