> **Historical provenance only.** This parked state is superseded by `LIVE-INTEGRATION-STATUS.md`; the current tree has a lazy live REAL CITY seam in `main.js` and first-class artistic review.

# JWEB visual harness — inert repository cut

This repository cut intentionally adds only `tools/visual-harness/`.

## Safety boundary

- `main.js` is not edited by this cut.
- No streaming, generator, physics, publication, boot, render-loop, or performance-path file is edited by this cut.
- The push ZIP is add-only and refuses to overwrite an already-existing `tools/visual-harness/` directory on the live clone.
- Full-world capture support remains implemented inside the tool, but the host seam is deliberately not installed here.
- Fixture mode, generator-specimen mode, browser self-test, Node tests, docs, and the practical implementation handoff are preserved in the repository.

## Why this shape

JWEB `main` is a moving target, and current performance work should not be coupled to a diagnostic-tool landing. The harness is therefore parked as a parseable, independently callable tool. A later cut can inspect the then-current runtime and hand-write the small host seam described in `INTEGRATION.md`.

## Included evidence

- Core visual-probe and capture code.
- Direct geometry-fixture adapter.
- Narrow Kowloon generator adapter.
- Browser and Node self-tests.
- `PRACTICAL-IMPLEMENTATION-LESSONS.md`, the real bug-capture handoff from the test drive.
- `ORIGIN-PARKED-STATUS.md`, the status note from the preservation bundle.
